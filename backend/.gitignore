node_modules
# Keep environment variables out of version control
.env

/lib/generated/prisma

# Vendor AI-assistant reference docs auto-installed by `prisma init` —
# not code we wrote or maintain, same reasoning as node_modules.
.agents
.claude
.windsurf
skills-lock.json
