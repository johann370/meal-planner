# File map

Every file and folder in this repo, one line each on what it is and why it
exists. Nothing in here should stay a mystery box.

## Status definitions

- **known** — I've explained what it is and why it exists, in my own words.
- **parked** — honest one-liner for now; a real deep-dive is scheduled for
  later (usually the build-plan section that introduces it).
- **generated** — machine-made. Never hand-edit; regenerated by a tool.

---

## `learning/`

Not part of the app itself — this is the record of the learning process:
what the project is, how it'll be built, what's actually understood so far,
and what every file in the repo does.

| Path | Status | What it is / why it exists |
|---|---|---|
| `learning/project.md` | parked | Who I am, the project idea, and the In-MVP / Parking-lot scope split. Deep dive: whenever scope is revisited. |
| `learning/plan.md` | parked | The five locked stack decisions and the 8-section build plan, sections only (no tasks yet). Deep dive: start of each new section. |
| `learning/knowledge-graph.md` | parked | Living map of what's actually known vs. just seeded, per concept — drives what gets quizzed. Deep dive: after the first real quiz. |
| `learning/file-map.md` | parked | This file. Deep dive: whenever a new top-level file/folder is added and needs explaining. |

## `.claude/`

Claude Code's own project-level config — not app code, not part of
`learning/` either. Holds tooling for how I'm used on this project.

| Path | Status | What it is / why it exists |
|---|---|---|
| `.claude/commands/next-task.md` | parked | A saved slash command (`/next-task`) — the full "read the plan, execute one task, teach as you go" prompt, so it doesn't need retyping/pasting each time. I wrote it at your request; deep dive whenever you want to read or tweak the wording yourself. |

## `backend/`

The Express server (Section 3), started from scratch with `npm init` (task 3.1) — no scaffolding tool, unlike `frontend/`. Sits alongside `frontend/` as its own separate project.

| Path | Status | What it is / why it exists |
|---|---|---|
| `backend/package.json` | known | Authored via `npm init` (task 3.1); now lists Express as a dependency (task 3.2). Task 7.2: gained a `devDependencies` block (Jest, Supertest, installed via `--save-dev`) separate from `dependencies`, and `scripts.test` now runs `jest` instead of the `npm init` placeholder. 2026-08-28 (ad hoc dev tooling): gained `nodemon` as a devDependency and a `"dev": "nodemon index.js"` script — used instead of `node index.js` locally so backend file changes auto-restart the server. Section 13 (task 13.1): gained `cheerio` (a real `dependencies` entry, not `devDependencies` — used at runtime by a route, not just for testing) for server-side HTML parsing. Section 15 (task 15.1): gained `prisma` (`devDependencies`, the CLI) and `@prisma/client` + `@prisma/adapter-pg` (`dependencies`, what actually runs in the deployed server), all pinned to the exact same stable `7.10.0` after a first install accidentally pulled a mismatched release candidate. Section 15 (task 15.5, 2026-08-29): lost `pg` via `npm uninstall pg` — nothing in the backend required it directly anymore once every route ran through Prisma. Correctly predicted the removal and that the app's behavior wouldn't change; `pg` itself stayed in `node_modules` regardless, since `@prisma/adapter-pg` still depends on it internally (a transitive, not direct, dependency now). |
| `backend/index.js` | known | Authored solo (tasks 3.3–6.2): Express server with full recipe CRUD (`GET`/`POST`/`PUT`/`DELETE /api/recipes`) and `PUT /api/week/:day` for day assignment; `/api/week` uses a `JOIN` across the split `recipes`/`week_meal` schema. All routes tested for real via `curl`. Task 6.5: added `POST /api/login` (`bcrypt.compare` against the stored hash, self-authored) and a `requireAuth` middleware (guide-authored) gating every route registered after it — reuses the same middleware-ordering trick as `cors`. Task 6.6: `cors()` narrowed from wildcard to `{ origin: 'http://localhost:5173', credentials: true }` (guide-authored) so the frontend's session cookie is actually accepted cross-origin. Task 7.1: shrunk to just `const app = require('./app'); app.listen(...)` — self-authored, including catching (after a prompt) a first attempt that called `require('./app')` without assigning it to anything. Task 8.3: self-authored an `if (process.env.PORT) {...} else {...}` branch around `app.listen` (Render assigns its own port; hardcoding `3000` would only have worked by luck, via Render's undocumented common-port fallback scan) — correctly predicted the failure mode before writing the fix, correct on the first try, confirmed live by starting it with `PORT=5050` and getting a real response back on that exact port. Section 14 (task 14.1): stale `TODO(you)` comment removed, verified correct first. |
| `backend/app.js` | known | Authored (task 7.1): all the Express app/route setup that used to live in `index.js`, minus `app.listen` — ends with `module.exports = app` instead, so Supertest can hand requests straight to the app object in tests without a real server needing to be running. Content is the guide's mechanical relocation of code the student already wrote; the split itself (and the resulting `index.js`) is what's new. Task 7.3: now also exposes `app.pool = pool` (guide-authored) so tests can close the DB connection pool when they're done. Task 7.5 aside: self-spotted that `POST /api/recipes` returned `200` instead of the more correct `201 Created` for a route that makes a new resource; self-authored the one-line fix (`res.status(201).json(...)`) and updated the matching test expectation. Task 7.7: self-authored an `if (process.env.NODE_ENV === 'test')` conditional around the `dotenv.config()` call — loads `.env.test` under Jest, `.env` otherwise — correct on the first try, no fixes needed. Task 8.1: self-authored swapping the hardcoded CORS origin for `process.env.CORS_ORIGIN`, correct on the first try. Task 8.3: self-authored `poolConfig`'s `if (process.env.DATABASE_URL) {...} else {...}` branch (Render's connection string vs. local discrete fields), including self-catching and fixing a real block-scoping bug (`const poolConfig` redeclared inside each branch, invisible outside both — fixed by hoisting one `let poolConfig;` above the `if/else`). Deployed to Render as a Web Service; live end-to-end (login → session cookie → real DB-backed `/api/week` response) confirmed over the public HTTPS URL after fixing a misconfigured `DATABASE_URL` env var (found via a real `getaddrinfo ENOTFOUND` error in Render's logs). Task 10.3 (in progress): `GET /api/recipes` now joins in each recipe's real `ingredients` rows (guide-authored object-spread skeleton, self-authored the `.filter()` match). `POST /api/recipes` self-authored past three real bugs — a missing `const` (silently created a global instead of erroring), a missing `return` inside a `.map()` callback (silently discarded every insert's promise), and an un-`return`ed nested `Promise.all(...).then(...)` chain that let a `TypeError` become an unhandled rejection and **crash the whole server process** — all found live via real `curl` tests and server-log stack traces, not guessed at. `DELETE /api/recipes/:id` now deletes a recipe's `ingredients` rows before the recipe itself (self-authored correctly on the first try, applying the just-learned `return`-the-chain fix unprompted). Task 10.3 (complete, 2026-08-27): `PUT /api/recipes/:id` self-authored to match — updates `recipes`' own columns (title/instructions only, no more writing to the dead `ingredients` text column), then deletes and re-inserts the recipe's `ingredients` rows fresh, mirroring `POST`. Built past two real, self-caught bugs: a stray semicolon that turned `.then()` into an invalid new statement, and a `const updatedRecipe` declared inside one `.then()` callback but referenced in a later sibling callback (a `ReferenceError`, fixed by hoisting a `let updatedRecipe;` above the whole chain — same fix as the `poolConfig` bug back in task 8.3). Confirmed live via `curl`, including a real "does replacing actually wipe the old rows" check. Task 10.6: new `GET /api/grocery-list` route added right after `/api/week` — same 3-way `JOIN` shape (`week_meal` → `recipes` → `ingredients`), extended with `GROUP BY ingredients.name, ingredients.unit` + `SUM(ingredients.quantity)` to combine matching ingredients (first use of SQL aggregation in the project). Query demonstrated live in `psql` first, then self-transcribed into the route's `TODO(you)` blank, correct on the first try. Confirmed via `curl`: 14 combined ingredients for the current week, `chicken breast` correctly totaled to 4 pounds. 2026-08-28 (ad hoc dev tooling): `requireAuth` now bypasses login in local dev only — `!process.env.DATABASE_URL && process.env.NODE_ENV !== 'test'`, reusing the same "not on Render" signal `poolConfig` already relies on, with Jest explicitly excluded so the real 401/login tests keep exercising real auth. Confirmed via a session-less request succeeding locally and the full 6-test suite still passing unchanged. One near-miss along the way, self-corrected before it was ever live: an in-between save briefly had `NODE_ENV !== 'test'` without the `process.env.` prefix (would have thrown a real `ReferenceError` on every request) — the version actually saved to disk already had it right. Section 13 (task 13.1): new `POST /api/recipes/import-from-url` route — fetches an external recipe URL's raw HTML and extracts its embedded JSON-LD via `cheerio`. Self-authored the one blank past a real, self-caught bug (`.first` written without its call parentheses). Confirmed live via `curl` against a real Budget Bytes recipe page. Task 13.2: now finds the actual `Recipe` object in the `@graph` array (first self-authored `.find()`) and reduces it to `{title, ingredientsRaw, instructions}` — `recipeInstructions`' step objects joined into one string via `.map()` + `.join()` (first use of `.join()` in the project). Confirmed live: exact title match, 10 real ingredient strings, instructions correctly combined. Task 13.3: new `stripTrailingParenthetical` (the app's first-ever regex) and `parseIngredient` (regex + `.split()` + destructuring + a `fractionMap` lookup) turn a raw string like `"1 lb spaghetti ($0.98)"` into `{quantity: 1, unit: "lb", name: "spaghetti"}`. Built past two self-caught bugs, each correctly predicted before running: a regex character-class mistake (`[^*]` instead of `[^)]*`), and a naive `.split(' ')` breaking on real double-space source data. Confirmed against all 10 real ingredient lines from the test recipe, with genuine `number`-typed quantities (including Unicode fractions ¼/½/¾/⅛ converted to decimals). `fractionMap` extended on request to also accept a `"N/D"` text form for each fraction. Task 13.4: `POST /api/recipes`'s insert logic extracted into a shared `createRecipe()` function, reused by both it and the new import route (first backend use of the "extract shared logic" pattern already practiced on the frontend). One real, self-diagnosed async bug: `POST /api/recipes` initially tried to respond with `createRecipe(...)`'s return value directly, without `.then()` — that's a `Promise` object, not the resolved recipe; self-fixed once asked what the function actually returns synchronously. Confirmed via `npm test` (still 6/6 passing after the refactor) and a real, live end-to-end import — `201 Created`, a genuine saved recipe with 10 correctly parsed ingredients, now a permanent row in the dev database. Extended right after: `fractionMap` also accepts HTML-entity fraction forms (`&frac12;`, `&frac14;`, `&frac34;`, `&frac18;`) — found via a real second-site test (Cookie and Kate) that surfaced `NaN` quantities the original map couldn't handle. A third-site test (Allrecipes) surfaced a real architectural limit — that site's structured data is added client-side by JavaScript, absent from the raw HTML a plain `fetch()` gets — root-caused via a real `curl`+`grep` check; fixed with a clear-error guard (`if (!jsonLdText) throw new Error(...)`) instead of a confusing crash, confirmed live. Section 13 (task 13.5): hardened against a fourth JSON-LD shape (The Kitchn's one tag has no `@graph` wrapper at all) via `(jsonLd['@graph'] || [])` plus a self-authored `if (!recipeData) throw new Error(...)` guard mirroring the existing one. `stripTrailingParenthetical` (task 13.3) fixed for a real nested-parentheses bug found unprompted (`"...cheese (shredded, (1 cup) $0.97****)"`) — `[^)]*` (can't cross any `)`) swapped for greedy `.*` (matches to the *last* `)` in the string), confirmed against every previously-tested ingredient line with zero regressions. Section 14 (task 14.1): the 2 remaining stale `TODO(you)` comments (`poolConfig`, `requireAuth`'s dev bypass) removed, both verified correct first; all 6 tests and a live `curl` confirmed nothing broke. Task 14.2: `stripTrailingParenthetical`, `fractionMap`, and `parseIngredient` moved out to `lib/recipeParser.js`; self-authored the `require()` wiring past one real, correctly-predicted bug (first attempt omitted the `./`, which would have looked for an installed package instead of the local file). Task 14.3 (biggest refactor in the section): shrank from ~200 to ~55 lines — every route moved out to `routes/` (see those entries above), leaving just config, `pool` setup, middleware (`cors`/`express.json`/`session`/`requireAuth`), and the 4 mounting lines, all self-authored. Confirmed via all 6 tests plus a full live smoke test across every route group. Extended right after, on request: `requireAuth` moved out to `middleware/requireAuth.js` too (see that entry above) — self-authored end to end. Extended once more: `Pool`/`poolConfig` moved to `lib/db.js` (see that entry above) — also self-authored, past two self-caught bugs. Extended a final time, on request: every `require()` grouped at the top (inline route requires pulled into named constants) and every `app.use()` grouped together right after `const app = express()` — guide-authored (pure reordering, no new logic), preserving the real constraint that the `app.use()` calls' *relative order* still can't change. Section 15 (task 15.2, 2026-08-29): `requireAuth`'s local-dev bypass condition swapped to the purpose-built `LOCAL_DEV_BYPASS_AUTH` flag, self-authored. Task 15.3: now also requires `lib/prisma.js`, passes `prisma` into `groceryListRoutes`/`recipesRoutes`/`weekRoutes` alongside `pool`, and exposes `app.prisma = prisma` (self-authored, mirroring the existing `app.pool = pool`) so tests can close its connection too. Task 15.5 (2026-08-29): `pool` fully retired from this file. Deliberately left the three mounting calls (`recipesRoutes(pool, prisma)` etc.) unchanged for one step after the route files' own signatures had already dropped to just `(prisma)`, specifically to observe the resulting bug (see `lib/db.js`'s entry) — then fixed by dropping back to a single `prisma` argument on each. `require('./lib/db.js')` and `app.pool = pool` deleted outright once nothing else in the file needed `pool` at all. |
| `backend/.env.test` | known | Authored (task 7.7): same 7 keys as `.env`, `DB_NAME` changed to `meal_planner_test`. Loaded instead of `.env` whenever `NODE_ENV=test` (which Jest sets automatically). Git-ignored via the root `.gitignore`'s `.env` rule alongside the real one. Task 8.1: gained `CORS_ORIGIN`, kept in sync with `.env` even though tests don't exercise CORS. Section 15 (task 15.1): gained `DATABASE_URL`, pointed at `meal_planner_test` — Prisma needs one connection-string, unlike the discrete `DB_*` fields the rest of the app still uses. |
| `backend/prisma/schema.prisma` | known | Authored (Section 15, task 15.1) via `prisma db pull` — introspected directly from the real `meal_planner` database, not hand-written. 3 models (`recipes`, `ingredients`, `week_meal`) matching the real tables exactly. First introspection attempt failed: the real `recipes` table still had its old, dead, pre-Section-10 `ingredients` text column, colliding with the real `ingredients` relation field of the same name — confirmed genuinely dead via `psql` (`"TBD"` placeholders, blanks, one known garbled leftover), then permanently dropped from both databases with explicit go-ahead. `generator client`'s `provider` set to `prisma-client-js` (the legacy generator) rather than v7's new default `prisma-client`, which emits raw TypeScript/ESM this plain CommonJS backend can't load at all — found by reading Prisma's own bundled migration docs, not guessed at. Section 15 (task 15.6, 2026-08-29): `recipes` gained a real new field, `created_at DateTime? @default(now())` — self-authored past one caught mistake (`@optional`, not a real Prisma attribute, corrected to the `?` suffix already used on `title String?` two lines up). First real proof of the full Prisma Migrate workflow, done on top of a database that already existed (see `prisma/migrations/` below for the baselining story). |
| `backend/prisma/migrations/` | known | Created (Section 15, task 15.6, 2026-08-29) — Prisma's own versioned migration history, didn't exist before this task since `schema.prisma` came from introspection, not migrations. `0_init/migration.sql`: a real `CREATE TABLE` script for all 3 existing tables, generated purely from a schema comparison (`prisma migrate diff --from-empty --to-schema ...`, no database touched) and manually placed here, then recorded as already-applied via `prisma migrate resolve --applied 0_init` on both `meal_planner` and `meal_planner_test` — the documented "baselining" recipe for adopting Migrate on a database that already has real data, followed from Prisma's own reference docs given the destructive stakes of getting it wrong. `20260829222431_add_recipe_created_at/migration.sql`: the first *real* migration, a genuine `ALTER TABLE recipes ADD COLUMN created_at ...`, generated and actually applied by `prisma migrate dev --name add_recipe_created_at` against dev, then `prisma migrate deploy` against test once a real 3-test failure (`column ... does not exist`) revealed the migration hadn't reached the test database yet. `migration_lock.toml`: Prisma-generated, records which database provider (`postgresql`) the migration history is locked to — never hand-edited. |
| `backend/prisma7.config.ts` | known | Generated by `prisma init` (Section 15, task 15.1) — Prisma 7's new config file format, replacing the old implicit `.env`-based `DATABASE_URL` discovery. Reads `DATABASE_URL` via `dotenv/config`; points at `prisma/schema.prisma` and `prisma/migrations`. Untouched beyond what `prisma init` wrote. |
| `backend/.gitignore` | known | Created by `prisma init` (Section 15, task 15.1) — didn't exist before. Excludes `node_modules`, `.env`, and `/lib/generated/prisma` (redundant with the root `.gitignore`'s first two rules, but harmless — the two files stack). Task 15.7: extended to also exclude `.agents`, `.claude`, `.windsurf`, and `skills-lock.json` — the 71 vendor AI-assistant reference doc files `prisma init` scattered around (task 15.1), deliberately kept out of version control rather than committed, same reasoning as `node_modules`. |
| `backend/lib/generated/prisma/` | generated | Output of `npx prisma generate` (Section 15, task 15.1) — the actual Prisma Client code, regenerated from `schema.prisma` any time it changes. Git-ignored via `backend/.gitignore`. Never hand-edit. Task 15.6: confirmed live that this is a genuine snapshot, not something that auto-updates — a new `created_at` field was invisible in real API responses until `prisma generate` was rerun by hand, even though the database column already existed and the server had been restarted. |
| `backend/.agents/`, `backend/.claude/skills/`, `backend/.windsurf/skills/`, `backend/skills-lock.json` | parked | Auto-installed by `prisma init` (Section 15, task 15.1) — ~80 files of Prisma's own bundled reference documentation, written for AI coding assistants (Claude Code, Windsurf, generic agents) to consult when helping with Prisma tasks. Not code authored or read line-by-line; treated like `node_modules/` — vendor-provided, not built on directly. |
| `backend/lib/prisma.js` | known | Authored (Section 15, task 15.3, 2026-08-29), guide-authored — new Prisma-specific plumbing (the driver-adapter pattern), not a concept to practice. One shared `PrismaClient`, built with a `PrismaPg` adapter from `DATABASE_URL` (Prisma 7 requires an adapter; a bare `new PrismaClient()` throws), exported directly — same "one shared instance" shape as `lib/db.js`'s `pool`. Confirmed working via a real `prisma.recipes.findMany(...)` call returning live data before being wired into any route. |
| `backend/lib/db.js` | **retired** | Authored (Section 14, task 14.3 extension, 2026-08-28), entirely solo — the `Pool`/`poolConfig` setup, moved out of `app.js`. Deleted (Section 15, task 15.5, 2026-08-29) once every route was on Prisma and nothing required it anymore — the raw `pg` `Pool` it built was fully dead weight. Deletion done last, in the correct order: `pool` first removed from all three route factories' signatures and from `app.js`'s calls/`require`/`app.pool` assignment and `app.test.js`'s `afterAll`, confirmed via tests at each step, *then* the file itself deleted. Along the way, self-authored past a real bug caught on purpose: with `app.js` still calling routes as `(pool, prisma)` but a route's signature already down to just `(prisma)`, the single parameter caught `pool` (JS binds arguments by position, not name) — reproduced live via a real `TypeError` and 3 failing tests before being fixed. |
| `backend/middleware/requireAuth.js` | known | Authored (Section 14, task 14.3 extension, 2026-08-28), entirely solo — function body, `module.exports = {requireAuth}`, and the `app.js` wiring, no scaffolding at all. One self-caught bug: first attempt required it via a leading `/` with no `.` (`/middleware/requireAuth.js`, meaning the filesystem root) instead of `./middleware/requireAuth.js`; self-diagnosed once asked to compare the two forms. Section 15 (task 15.2, 2026-08-29): the local-dev bypass condition swapped from the now-unreliable `!process.env.DATABASE_URL` to the purpose-built `process.env.LOCAL_DEV_BYPASS_AUTH` flag, self-authored correct on the first try — including unprompted reasoning that the old condition's `NODE_ENV !== 'test'` half was now redundant (the new flag simply isn't present in `.env.test`) and could be dropped. Confirmed via all 6 tests plus a live no-session `curl` to `/api/week`. |
| `backend/routes/auth.js` | known | Authored (Section 14, task 14.3): `POST /api/login`, split out of `app.js` — guide-authored relocation of the student's own existing logic. Exports a factory function (no `pool` needed) returning an `express.Router()`; mounted in `app.js` *before* `requireAuth`, self-authored, preserving the same critical ordering the single-file version relied on. Task 14.6: converted to `async`/`await` + `try`/`catch`, self-authored correct on the first try — proactively added a `catch` block that hadn't existed before. |
| `backend/routes/week.js` | known | Authored (Section 14, task 14.3): `GET /api/week` and `PUT /api/week/:day`, split out of `app.js` — guide-authored relocation. Exports a factory function taking `pool`, returning an `express.Router()`; mounted in `app.js` after `requireAuth`, self-authored. Task 14.6: converted to `async`/`await` + `try`/`catch`, self-authored correct on the first try, both routes. Its `SELECT` reformatted across multiple lines with a backtick template literal, on request. Section 15 (task 15.3, 2026-08-29): `GET /api/week` converted to `prisma.week_meal.findMany({include: {recipes: true}, orderBy: {id: 'asc'}})`. Correctly reasoned, unprompted, that Prisma's `include` keeps a row with no matching recipe (`recipes: null`) instead of silently dropping it like the old inner `JOIN` — added `.filter(row => row.recipes)` before mapping to `{day, meal}`, closing a real crash risk that raw SQL had accidentally been protecting against. One self-caught, correctly-predicted bug: `.filter()` chained straight onto `findMany(...)` before `await`-ing it — a `TypeError`, since a Promise has no `.filter()`. `PUT /api/week/:day` also now on Prisma (`prisma.week_meal.findFirst` + `.update`) — found already converted at the start of task 15.4, undocumented until now; confirmed working via all 6 tests. Section 15 (task 15.5, 2026-08-29): `pool` dropped from the factory signature entirely (`(prisma)` now, `app.js` updated to match) — part of the real bug this task demonstrated live (see `lib/db.js`'s entry). A second, unrelated real bug found during this task's own live click-through: `PUT /api/week/:day` received `recipeId` as a string (every `<select>`'s `onChange` value is text, even for numeric ids) and Prisma's typed client rejected it outright — the old raw SQL had silently tolerated the same mismatch. Fixed by wrapping it in `parseInt(recipeId)`, matching `recipes.js`'s existing convention; self-authored correct on the first try, confirmed via `curl`, all 6 tests, and a real second click-through. Section 20 (2026-09-01): `GET /api/week`'s `.filter(row => row.recipes)` replaced with a ternary inside the `.map()` (`row.recipes ? {day, meal: row.recipes.title} : {day, meal: null}`), self-authored, so an unassigned day now comes back with `meal: null` instead of vanishing from the response entirely. `PUT /api/week/:day` now stores `recipeId ? parseInt(recipeId) : null` instead of an unconditional `parseInt(recipeId)` — self-authored, replacing an accidentally-working case (`parseInt(null)` → `NaN`, which Prisma happened to silently store as `NULL` anyway) with explicit intent, a deliberate choice once the coincidence was found and explained. New `DELETE /api/week/meals` route, entirely self-authored with no scaffolding: `prisma.week_meal.updateMany({data: {recipe_id: null}})` (first use of `updateMany` in the project — omitting `where` updates every row) plus a `204` response matching `recipes.js`'s existing convention. All three confirmed via `curl` and direct `psql` checks; real dev data (`Monday: 2, Tuesday: 7, Wednesday: 7, Thursday: 4, Friday: 5, Saturday: 3, Sunday: 2`) intentionally wiped during testing and restored by hand afterward each time. |
| `backend/routes/groceryList.js` | known | Authored (Section 14, task 14.3): `GET /api/grocery-list`, split out of `app.js` — guide-authored relocation. Same factory-function/`Router` shape as `week.js`. Task 14.6: converted to `async`/`await` + `try`/`catch` — guide-authored as the first worked example, establishing the conversion pattern for every other route file. Its `SELECT` reformatted across multiple lines with a backtick template literal, on request. Section 15 (task 15.3, 2026-08-29), the hardest of the three GET conversions: Prisma can't express a `GROUP BY` across a multi-table `JOIN` directly, so the route now fetches nested (`week_meal`→`recipes`→`ingredients`, guide-authored) and combines in JS — first use of `.flatMap()` (flatten each day's ingredients into one list) and self-authored `.reduce()` (using the already-known `.find()` to detect and merge matching name+unit entries). Self-authored fix for a real gotcha: Prisma represents Postgres `numeric` columns as Decimal *objects*, not numbers — plain `+` string-concatenates them (confirmed live: `"1" + "0.5"` → `"10.5"`) — fixed with `parseFloat()`, an unprompted correct reuse of task 13.3's fraction-parsing tool in a new context. Confirmed the multiplication case works correctly (a recipe assigned to two different days sums its ingredients twice, not deduplicated). Self-added `.sort((a,b) => a.name.localeCompare(b.name))` after self-noticing the combined list had lost the old `ORDER BY`. Section 15 (task 15.5, 2026-08-29): `pool` dropped from the factory signature (`(prisma)` now) — see `lib/db.js`'s entry for the real bug this uncovered along the way. Section 17 (task 17.2, 2026-08-31): gained `unitSynonyms` (a lookup table, same shape as `lib/recipeParser.js`'s `fractionMap`) and `normalizeUnit(rawUnit)` — lowercases, strips a trailing period via a self-authored `.replace(/\.$/, '')` regex (correct on the first try, including an unprompted `$` end-anchor beyond what was explained), then looks up a canonical form or falls back to the cleaned value. Confirmed against 8 real unit strings from task 17.1's data. Extended right after, on request: plural forms (`tablespoons`/`teaspoons`/`ounces`) filled in to match every abbreviation-style unit already covered — guide-authored, a mechanical extension of the established pattern. Section 17 (task 17.3, 2026-08-31): self-authored a matching `normalizeIngredient(rawIngredient)` function (lowercase + trim, same shape as `normalizeUnit`), then rewrote the `.reduce()`'s `.find()` comparison to match on both normalized values instead of the raw ones — correct on the first try. Confirmed live: two real, differently-cased/differently-unitted spaghetti ingredients (from two different recipes, temporarily both assigned in the same week to make the test reachable) now combine into one grocery-list entry. Task 17.4 (2026-08-31): that quirk fixed — self-authored, ahead of any scaffold, swapping the create-new-entry branch's `ingredient.name`/`ingredient.unit` for their normalized forms, so the displayed label is now deterministic (every raw variant of the same ingredient collapses to one identical output) instead of depending on processing order. A deliberate, explicitly-discussed trade-off: proper-noun ingredient names (`"Italian parsley"`) now display lowercased too, in exchange for that consistency. Task 17.6: `unitSynonyms`/`normalizeUnit`/`normalizeIngredient` moved out to `lib/normalize.js`; self-authored the `require()` wiring back in, correct on the first try. Confirmed identical output via `curl` — a pure refactor. |
| `backend/routes/recipes.js` | known | Authored (Section 14, task 14.3): full recipe CRUD plus `POST /api/recipes/import-from-url`, split out of `app.js` — guide-authored relocation, including `createRecipe()` (now private to this file, since nothing else calls it). Requires `../lib/recipeParser.js` for `parseIngredient`. Same factory-function/`Router` shape as `week.js`. Task 14.6: the biggest `async`/`await` conversion, self-authored past two real bugs — a missing `await` on `pool.query(...)` in the `PUT` route (correctly predicted the crash before running it) and an incompletely-converted `Promise.all(...).then(...)` left inside `createRecipe`. Correctly predicted, unprompted, that `PUT`'s `updatedRecipe` no longer needs the `let`-hoisting trick from task 10.3. One regression caught by prediction: every route's `catch` had flattened to the same generic message, silently breaking Section 13's specific `"Could not get recipe data"` error — restored by matching the app's real original convention (`GET` generic, mutations pass through `err.message`). Its recipe/ingredient `INSERT`/`UPDATE` queries reformatted across multiple lines with backtick template literals, on request. Section 15 (task 15.3, 2026-08-29): `GET /api/recipes` converted to `prisma.recipes.findMany({include: {ingredients: true}, orderBy: {id: 'asc'}})`, self-authored correct on the first try, replacing the old two-query-plus-`.filter()` join. Task 15.4: every remaining write route is now on Prisma too — `createRecipe`, `PUT /api/recipes/:id` (nested `ingredients: {create/deleteMany}`), and `week.js`'s `PUT /api/week/:day` were already converted going into this task (real working code found already in place, undocumented until now); `DELETE /api/recipes/:id` was this task's actual new work, self-authored correct on the first try (`prisma.ingredients.deleteMany` then `prisma.recipes.delete`, preserving the same children-before-parent order the FK requires). Confirmed via all 6 tests plus a live create/update/delete `curl` cycle. Section 15 (task 15.5, 2026-08-29): `pool` dropped from the factory signature (`(prisma)` now) — part of the real "which argument binds to which parameter" bug this task deliberately triggered and fixed (see `lib/db.js`'s entry). Section 17 (task 17.7, 2026-08-31): now also requires `lib/normalize.js`; both `createRecipe` and `PUT /api/recipes/:id`'s ingredient `.map()`s self-authored to normalize `name`/`unit` before storing, so ingredients are clean at write time, not just when the grocery list reads them. Confirmed via a real create+update test (messy casing/units in, normalized values out, verified via `psql`), then cleaned up; all 6 tests still pass. |
| `backend/lib/normalize.js` | known | Authored (Section 17, task 17.6, 2026-08-31): `unitSynonyms` + `normalizeUnit` + `normalizeIngredient`, moved out of `groceryList.js` — guide-authored relocation (same shape as task 14.2's `recipeParser.js` split), unchanged logic. Exports both functions directly (unlike `recipeParser.js`'s private helpers, both are genuinely needed by more than one file — `groceryList.js` today, `recipes.js` next in task 17.7). |
| `backend/lib/recipeParser.js` | known | Authored (Section 14, task 14.2): the ingredient-parsing logic (`stripTrailingParenthetical`, `fractionMap`, `parseIngredient`) split out of `app.js` into its own file — guide-authored relocation, same shape as task 7.1's `app.js`/`index.js` split. Exports only `parseIngredient`; the two helpers it depends on stay private, since nothing outside this file calls them directly. Moved into a new `lib/` folder right after, on request — the first step toward a real backend folder structure, matching the frontend's own upcoming `components/` folder (task 14.4). |
| `backend/.env.render` | known | Authored (task 8.2): one key, `RENDER_DATABASE_URL` — the External Database URL for the new `meal-planner-db` Render Postgres instance, used to migrate schema/seed data and confirm connectivity from the local machine via `psql`. Automatically caught by the `.env.*` `.gitignore` rule added in task 7.8, with zero extra edits needed — that fix already paying off. Not meant to be read by the deployed app itself, which uses Render's *Internal* URL instead — that got wired in as the `DATABASE_URL` environment variable directly in the Render dashboard (task 8.3), not read from this file; this file stays a local-only admin/migration credential. |
| `backend/app.test.js` | known | Authored (task 7.2): first real test — `request(app).get('/api/week')` with no session, asserting a `401`. Self-authored and self-debugged (a missing space turned `await request` into one undeclared identifier, `awaitrequest`). Task 7.3: added two more self-authored tests (wrong password rejected; correct password + `request.agent(app)` unlocks a protected route), a test-only password hash (`process.env.ADMIN_PASSWORD_HASH` overwritten with a throwaway test password — the real password never enters this file), and `afterAll(() => app.pool.end())` to close the DB pool after tests (fixed a real "Jest did not exit" warning; correctly diagnosed the open `pg` connections as the cause before being told). Task 7.4: added a fourth test asserting `GET /api/week`'s response *shape* (array of `{day, meal}` objects) rather than exact values, self-authored past one real bug — first attempt called `.toHaveProperty('day')` on the whole array instead of `response.body[0]`, correctly predicted the failure and self-fixed after seeing the real Jest diff. Task 7.5: added a fifth test — the first *mutation* test (inserts a real DB row via `POST /api/recipes`), self-authored the `toBe`/`toHaveProperty` assertions on the create response; guide wrote the follow-up `GET /api/recipes` + `.find()` persistence check and the `DELETE` cleanup at the end (new syntax: `.find()`, template-literal URL) so the test doesn't leave junk data behind — confirmed via a real `psql` query after the run that the row was really gone. Task 7.6: added a sixth test — `PUT /api/week/:day` actually reassigns a day. First test needing "restore" cleanup instead of "delete" cleanup (a day's row always exists; only its recipe changes), guide-authored; self-authored the core assertion (`expect(mondayAfter.meal).toBe(newRecipe.title)`), correct on the first try. Confirmed via a real `psql` query after the run that Monday was back to "Beef Tacos" and the temp recipe was gone. Aside (8/16): self-spotted the repeated `agent = request.agent(app); await agent.post('/api/login')...` boilerplate across 4 tests as a DRY violation; guide added a module-scoped `beforeEach` to log in fresh before every test, self-removed the 3 now-redundant duplicate blocks (tests 4–6) in the editor, correctly leaving tests 1–3 untouched since they specifically test unauthenticated/login behavior itself. Mispredicted the resulting pass count (3/6, forgetting tests 1–2 don't use `agent` and test 3 declares its own local one) but correctly diagnosed the exact gap once asked. Task 10.3 (8/27): both recipe-creation test fixtures (`newRecipe` in tests 5 and 6) updated from a plain `ingredients` string — stale since `POST` moved to real ingredient rows — to a real `[{name, quantity, unit}]` array; this had been silently broken since earlier in task 10.3 and only surfaced once the suite was actually rerun. Section 15 (task 15.3, 2026-08-29): `afterAll` gained `await app.prisma.$disconnect()` alongside the existing `await app.pool.end()` — self-authored, correctly self-diagnosed unprompted as needed once a real "Jest did not exit" warning reappeared after adding the second (Prisma) database connection, reasoning directly from the same open-handle lesson task 7.3 first taught for `pool`. Task 15.5: `await app.pool.end()` deleted from `afterAll` — the pg `Pool` it was closing doesn't exist anymore, only `app.prisma.$disconnect()` remains. Section 20 (2026-09-01): two new self-authored tests for the day-unassign feature — `PUT /api/week/:day` with a null `recipeId` unassigns Tuesday (mirrors the existing assign test's note/change/restore shape, first use of `.toBeNull()` in the project), and `DELETE /api/week/meals` clears every day at once (captures the whole week generically via `.map()`, confirms every day's `meal` via `forEach`, then restores all seven — correctly handling a day with nothing to restore to via `recipe ? recipe.id : null`). One self-caught bug along the way: after hoisting a redundant `GET /api/recipes` call out of the restore loop, first wrote `recipes.find(...)` instead of `recipes.body.find(...)` — self-diagnosed once asked to compare it against the existing pattern elsewhere in the file. Both run against `meal_planner_test`, so no real dev data needed restoring afterward. Confirmed via `npm test`: 8/8 passing. |
| `backend/.env` | known | Authored (task 5.3): DB connection credentials (user/password/host/port/database name). Task 6.5: added `ADMIN_PASSWORD_HASH` (self-generated via `bcrypt.hashSync`, real password never shared with the guide) and `SESSION_SECRET` (guide-generated random hex, signs the session cookie). Task 8.1: added `CORS_ORIGIN` (currently `http://localhost:5173`, will become the deployed frontend's URL later). Git-ignored via the root `.gitignore`'s `.env` rule — never committed. Section 15 (task 15.1): `prisma init` auto-inserted a `DATABASE_URL`, but pointed at Prisma's own local dev Postgres server, not the real `meal_planner` database — replaced with the real connection string, built from the existing discrete `DB_*` fields already in this file. Also gained `LOCAL_DEV_BYPASS_AUTH=true`, a replacement signal for `requireAuth`'s local-dev bypass now that `DATABASE_URL` is no longer Render-only; wired up and confirmed working in task 15.2. |
| `backend/package-lock.json` | generated | Created by `npm install express` (task 3.2), updated by later installs (`pg`, `bcryptjs`, `express-session`). Exact locked dependency versions. Never hand-edit. |
| `backend/node_modules/` | generated | Installed dependencies (Express, pg, bcryptjs, express-session, and their dependency trees). Ignored by git, regenerable. |

## Project root

| Path | Status | What it is / why it exists |
|---|---|---|
| `.git/` | generated | Created by `git init` (task 1.1). Git's own storage for the entire commit history of this project. Hidden (dot-prefixed), never hand-edit — only Git itself writes here. |
| `.gitignore` | known | Authored (task 1.2): tells Git to never track `node_modules/` (regenerable via npm) or `.env` (secrets). Set up before either file exists. Task 6.6: added `cookies.txt`, after a stray `curl -c cookies.txt` cookie-jar file from testing the login route turned up untracked at the repo root; the file itself was deleted (disposable test output, not app code). Task 7.8: added `.env.*`, after a near-miss where the new `backend/.env.test` (real secrets, same shape as `.env`) turned up untracked and would have been committed — the plain `.env` line only ever matched that exact filename. Self-authored past one gap (first draft, `.env.*` alone, doesn't also cover the bare `.env` file) and independently rejected a broader one-line `.env*` fix as over-matching unrelated dotfiles. |
| `index.html` | known | Authored solo (task 1.5): the first real app page — a hardcoded fake week of recipes, plain HTML, no CSS/JS yet. Section 2 rebuilds this in React. |
| `SUPPORTED_SITES.md` | known | Authored (Section 13, 2026-08-28), on request: a real app reference doc (not a `learning/` file) listing which recipe sites `POST /api/recipes/import-from-url` is confirmed to work on, which fail and why (no JSON-LD at all vs. JSON-LD present but no `Recipe` type), and known parsing limitations — a snapshot from the real multi-site testing done this session, not a guarantee going forward. Corrected twice (task 13.5): Minimalist Baker and The Kitchn moved from a misleading "not yet tested" note to their real, confirmed "doesn't work" result (their JSON-LD has no `Recipe` type at all) — that earlier note had only checked the tag existed, not that it held real data; King Arthur Baking's entry updated to note it's now confirmed through the real UI, not just `curl`. |
| `/` (repo root) | parked | Holds `learning/`, `.claude/`, `.git/`, `index.html`, `SUPPORTED_SITES.md`, and `frontend/`. Section 1's static page lives at root; the React rebuild (Section 2) lives in `frontend/`. |

## `frontend/`

The React app (task 2.1), scaffolded with Vite. Separate folder from the repo root on purpose — `backend/` will sit alongside it in Section 3, keeping frontend/backend as distinct concerns.

| Path | Status | What it is / why it exists |
|---|---|---|
| `frontend/package.json` | parked | Project manifest — lists dependencies (React, Vite) and npm scripts (`npm run dev`, etc.). Deep dive: whenever we add a new dependency. |
| `frontend/index.html` | parked | Stripped-down HTML shell with an empty `<div id="root">` and a script tag loading `src/main.jsx`. React fills that div at runtime. |
| `frontend/src/main.jsx` | known | Entry point — finds `#root` and tells React to render the app into it. Section 18 (task 18.2, 2026-08-31): the `import './index.css'` line removed (self-authored, one-line deletion) once `index.css` itself was deleted — confirmed it was the file's only reference first. |
| `frontend/src/App.jsx` | known | Authored (tasks 2.2–4.4): week-of-recipes JSX; `week` is real backend-fetched state (`useEffect` + `fetch`), CORS unblocked — genuinely live end-to-end. Task 6.3: now also renders `<RecipeManager />` below the week list (first multi-component composition; the import/render wiring was written by the guide, not authored solo). Task 6.4: each day now has a `<select>` dropdown (built from fetched `recipes` state) that `PUT`s an assignment via `handleAssign` and re-`fetchWeek()`s to refresh — authored solo, correct on the first try, no bugs. Also self-extracted `fetchRecipes` as a named function mirroring `fetchWeek`, unprompted. Task 6.6: gated behind `loggedIn` state — an early-return `if (!loggedIn) return <Login .../>` and a `useEffect` dependency array changed from `[]` to `[loggedIn]` (both guide-authored); all three of its own `fetch` calls gained `credentials: 'include'` (`fetchWeek` self-fixed, `fetchRecipes`/`handleAssign` guide-swept). Task 8.1: all three `fetch` calls' hardcoded `http://localhost:3000` swapped for `` `${import.meta.env.VITE_API_URL}` `` (guide-swept, same pattern the student proved solo in `Login.jsx`). Task 9.1: self-authored passing `recipes`/`setRecipes` down as props on `<RecipeManager>` (`recipes={recipes} setRecipes={setRecipes}`), the first half of the shared-state bug fix — correct on the first try. Section 14 (task 14.4): all three imports updated to the new `./components/` prefix, self-authored past two self-caught bugs — first attempt left them entirely unchanged, second had a typo (`.components/...`, missing the `/` after the `.`).
Section 13 (task 13.5): new "Import from URL" control (`importUrl` state, `handleImport`), self-authored past a real stray-comma bug splitting `fetch(url, {...})` into two disconnected pieces. Correctly reasoned, unprompted, that it belongs entirely outside the existing create-recipe `<form>`. Also fixed a real reproduced crash: `handleImport` originally pushed an error-response object straight into `recipes` with no check, and rendering it blanked the whole page — self-authored an `if (newRecipe.error) { alert(...); return; }` guard, adding the `alert()` once asked whether silent failure was intended. Task 10.7: gained the import + `<GroceryList />` render (guide-authored, same mechanical-wiring split as `RecipeManager`'s original composition). Section 12 (bug fix): `<GroceryList>` now receives `week` as a prop, self-authored, so it has a real signal to refetch on. Section 16 (task 16.1, 2026-08-30): the instructions field's `<input>` swapped for a `<textarea>`, self-authored past a left-behind `TODO(you)` marker, correct on the first try. Task 16.2: confirmed live that a real multi-line recipe's line breaks survive a full save → Edit-reload round trip unchanged, as predicted from the field being a plain text column. Task 16.4: the `<textarea>` gained `className="instructions-input"`, self-authored, so `App.css` could give it a real height and vertical-only resizing instead of a cramped single-input-sized box. Section 18 (task 18.2, 2026-08-31): `<h1>` gained `id="weekly-meals-title"` (self-authored, at the student's request, so its centering could be scoped past a global `h1` selector that would've also affected `Login.jsx`'s own heading); day names now displayed via `day.day.slice(0, 3)` (a display-only abbreviation — the real `"Monday"` value is untouched, still used for the `PUT /api/week/:day` call); `.week`, `RecipeManager`, and `GroceryList` wrapped in a new `<div className="main-layout">` for a side-by-side layout. Section 19 (2026-08-31): the per-day `<select>` removed entirely; `RecipeManager` now conditionally rendered (`{selectedDay && <RecipeManager .../>}` — first use of this inline conditional pattern in the project) and receives `selectedDay`/`handleAssign` as new props, so recipe assignment now happens by clicking a recipe instead of choosing from a dropdown. Section 19 (deselect, task 1, 2026-09-01): now also passes `onClose={() => setSelectedDay(null)}` into `<RecipeManager>`, self-authored correct on the first try — same callback-prop shape as `onLogin`. Section 19 (deselect, task 2, 2026-09-01): gained `weekRef`/`recipeManagerRef` (`useRef(null)`, the app's first use of the hook), `weekRef` attached directly to `<ul className="week">`, `recipeManagerRef` threaded down to `<RecipeManager>` as an ordinary prop, and a new `useEffect` registering a `document`-level click listener (with a `return`ed cleanup — the app's first real use of a `useEffect` cleanup function) that calls `setSelectedDay(null)` whenever a click lands outside both refs. All self-authored from a plain-language requirements list, including self-catching and fixing a real `recipeManagerRef.current` null-crash risk (it's `null` whenever `RecipeManager` isn't mounted). Confirmed live: outside clicks deselect, day-switching still works, and clicks inside `RecipeManager` (including its buttons) don't accidentally trigger a deselect. Section 20 (2026-09-01): the meal `<p>` now shows a self-authored `day.meal ? day.meal : "+ Add Recipe"` placeholder instead of rendering blank. A new `X` unassign button (gated on `day.meal`, so it only shows when there's something to unassign) calls `handleAssign(day.day, null)` — self-caught a real bug before running it (`day` is the whole loop-variable object, not the day string) and self-authored the `stopPropagation` fix for the same nested-click-inside-a-clickable-`<li>` bubbling issue as Section 19. New `handleClearWeek` function and a "Clear Week" button (sibling of `<ul className="week">`) call the new `DELETE /api/week/meals` route — both self-authored. Correctly predicted, unprompted, that this button — sitting outside both `weekRef` and `recipeManagerRef` — also triggers the click-outside-deselect listener from Section 19 as a side effect, on top of clearing the week. |
| `frontend/vite.config.js` | parked | Tells Vite this is a React project so it knows how to handle JSX. |
| `frontend/.gitignore` | parked | Vite-generated, scoped to this subfolder — redundant with the root `.gitignore`'s `node_modules/` rule but harmless. |
| `frontend/package-lock.json` | generated | Exact installed dependency versions. Never hand-edit. |
| `frontend/node_modules/` | generated | Installed dependencies (via `npm install`). Ignored by git, regenerable. |
| `frontend/.oxlintrc.json` | parked | Config for Oxlint, the linter chosen during scaffolding. |
| `frontend/public/` | parked | Static assets served as-is (favicon, icons). |
| `frontend/src/App.css` | known | Authored (tasks 2.3–2.4): `.week`/`.day` flexbox card layout, plus `.day.selected` for the click-to-highlight state (yellow border). Section 11 (task 11.3): gained `.recipe-view` and its children — a centered, bordered cookbook card, self-authored mirroring `.day`'s existing border/padding pattern. Hit a real gap: `max-width` alone doesn't center a block element, since its `margin` defaults to `0` (hugs the left edge) — fixed via `margin: 0 auto`, refined from the shorthand `margin: auto` unprompted. Section 16 (task 16.3, 2026-08-30): gained `.recipe-view .instructions { white-space: pre-line; }`, self-authored correct on the first try, nested to match the file's own `.recipe-view h2`/`.recipe-view li` pattern — the fix that makes a recipe's already-correct multi-line `instructions` string actually render as separate lines instead of one run-on paragraph (a browser default, not a data problem). Task 16.4: gained `.instructions-input { height: 4em; resize: vertical; }`, self-authored correct on the first try — correctly predicted, before running, that `resize: vertical` allows dragging for height only, never width, confirmed live. Section 18 (task 18.2, 2026-08-31): `.week`/`.day` reworked through a real multi-round debugging arc — `.day` now `flex: 1;` (simplified back from an intermediate `flex: 0 1 160px;`) plus `min-width: 0;` (overrides the flex-item auto-shrink floor that was blocking `nowrap` from working); `.week` now `flex-wrap: nowrap;` plus a self-added `padding: 0;` (removes a `<ul>` default left-indent that used to be silently absorbed by the now-deleted `index.css`); new `.day select { width: 100%; }` so the day-assignment dropdown actually shrinks with its parent instead of forcing overflow. See `plan.md` task 18.2 for the full diagnostic chain (flex-grow-per-line, the `min-width: auto` floor, `#root`'s deleted width cap, and the `<ul>` padding discovery). |
| `frontend/src/components/RecipeManager.jsx` | known | Moved into a new `components/` folder (Section 14, task 14.4, 2026-08-28), entirely self-authored — its own internal `import RecipeView from './RecipeView.jsx'` correctly needed zero changes (both files moved together), confirmed after one initially-wrong prediction corrected through reasoning about what a relative path actually describes. Authored (task 6.3): full CRUD UI for recipes — fetches and lists `/api/recipes`, a controlled form (create + edit, same form/handler reused via `editingId` state) doing real `POST`/`PUT`/`DELETE` calls. Debugged two real bugs solo: a `setRecipe`/`setRecipes` typo caught via a browser-console `ReferenceError`, and a missing `else` that was silently double-submitting on edit (POST *and* PUT both firing). Task 6.6: all four `fetch` calls gained `credentials: 'include'` (guide-authored sweep) so they keep working once the app is behind the login gate. Task 8.1: all four `fetch` calls' hardcoded `http://localhost:3000` swapped for `` `${import.meta.env.VITE_API_URL}` `` (guide-swept, same pattern the student proved solo in `Login.jsx`). Task 9.2 (shared-state bug fix, second half): self-authored the signature change to `({recipes, setRecipes})`, deleted the component's own now-redundant `useState` for recipes, and deleted its own redundant fetch `useEffect` (plus the now-unused `useEffect` import) — all solo, ahead of guidance on two of the three changes. `recipes`/`setRecipes` now come from `App` as props instead of a separate local copy; every existing handler (`handleDelete`, `handleSubmit`, etc.) needed zero changes since they already referred to those names. Task 10.4 (complete, 2026-08-28): the create form's single `ingredients` string replaced with a real array of `{name, quantity, unit}` objects — one starting blank row, rendered via `.map()` into a `Fragment`-keyed group of 3 inputs per row (each immutably updating just its own field), plus working "Add Ingredient" (append) and "Delete Ingredient" (`.filter()` by index) buttons, both self-authored. Built past several real bugs along the way (see knowledge graph for the full list) — most notably a form `<button>` with no `type` accidentally submitting the whole form. Confirmed live end-to-end: created a real 2-ingredient recipe through the form. Bonus finding: `handleEditClick`'s `setIngredients(recipe.ingredients)` (line 24), previously assumed broken and deferred to task 10.5, turns out to already work correctly with zero extra code — `GET /api/recipes` already returns `ingredients` in the exact shape the form expects. Task 10.5 (complete, 2026-08-28): the recipe `<li>` in the list now shows each recipe's ingredients via a self-authored nested `.map()` (the app's first list-inside-a-list), keyed by `ingredient.id` — a real database id, the first time a key wasn't just an array index. Correctly predicted, unprompted, that the choice of key has no visual effect, only internal bookkeeping. Section 11 (task 11.2): gained a `viewingRecipe` state and an early-return to `<RecipeView>` when set; the recipe title is now a clickable `<span>` opening its cookbook view. One self-caught bug: the title's `onClick` initially passed `true` instead of `recipe` to the state setter — spotted and fixed before it was ever run. Section 18 (task 18.1, 2026-08-31): the always-visible nested ingredient list (task 10.5) removed from the recipe `<li>` — self-authored, clean deletion, now redundant since `RecipeView` shows ingredients properly on click. List shows just title + Edit/Delete. Section 19 (2026-08-31): now receives `selectedDay`/`handleAssign` as props; the recipe `<li>` itself carries the assign `onClick`, a new "View" button restores the old view-recipe behavior (moved off the title), and Edit/Delete/View all gained `event.stopPropagation()` to stop their clicks from also bubbling up to the `<li>`'s assign handler — the project's first encounter with event bubbling. The View button's fix self-authored correct on the first try; Edit/Delete's identical fix guide-applied once the pattern was proven understood. Section 19 (deselect, task 1, 2026-09-01): now also receives `onClose` as a prop and renders a `<button onClick={() => onClose()}>Close</button>` right under the "Manage Recipes" heading — both self-authored correct on the first try, no hints beyond the design already agreed the prior session. Two real reasoning checks passed along the way: correctly recognized, once asked to compare against `RecipeView.jsx`'s own `onClick={onClose}` Close button, that wrapping it in a new arrow function makes no behavioral difference here (`onClose` takes no parameters, so the click event React auto-passes to any `onClick` handler is simply ignored either way); and correctly reasoned, unprompted once pointed at the JSX structure, that the missing `type="button"` on this button doesn't matter since it sits as a sibling of `<form onSubmit={handleSubmit}>`, not a descendant — task 10.4's "button defaults to submit" rule applied in reverse, recognizing where it *doesn't* apply. Confirmed live: selecting a day and clicking Close correctly un-highlighted the day and hid `RecipeManager`, exactly as predicted beforehand. Simplified right after, self-initiated: swapped `onClick={() => onClose()}` for the bare `onClick={onClose}` (matching `RecipeView.jsx`'s own Close button), applying the just-confirmed equivalence unprompted. Section 19 (deselect, task 2, 2026-09-01): now also receives `recipeManagerRef` as a prop and attaches it via `ref={recipeManagerRef}` on the root `<div className="recipe-manager">`, self-authored correct on the first try. |
| `frontend/src/components/RecipeView.jsx` | known | Moved into `components/` (Section 14, task 14.4) alongside `RecipeManager.jsx`, self-authored, no import changes needed. Authored (task 11.1): the app's first purely presentational component — no `useState`/`useEffect` of its own, just a `recipe` and an `onClose` callback arriving as props. Renders the title, an ingredients list (`ingredient.id` as key — self-correctly distinguished from the id-less grocery-list data), and the instructions, plus a Close button. Wired into `RecipeManager.jsx` (task 11.2) via a new `viewingRecipe` state and an early-return pattern (`if (viewingRecipe) return <RecipeView .../>`) echoing `App.jsx`'s own login gate. One bug caught before ever running: the title's `onClick` initially passed `true` instead of the real `recipe` object — self-spotted once asked to trace the consequence. Styled (task 11.3) as a centered, bordered card in `App.css`'s `.recipe-view` rules, past a real "why isn't `max-width` centering this" gap, fixed with `margin: 0 auto`. Confirmed live end-to-end (task 11.4). Section 16 (task 16.3, 2026-08-30): the instructions `<p>` gained a `className="instructions"` so `App.css` could target it — self-authored, correct on the first try — fixing the real gap where its already-correct multi-line data (task 16.2) still rendered as one run-on line, a pure browser-rendering issue, not a data issue. |
| `frontend/src/components/GroceryList.jsx` | known | Moved into `components/` (Section 14, task 14.4), self-authored. Authored (task 10.7): fetches `/api/grocery-list` on mount (own `useState`/`useEffect`/`fetch`, mirroring `RecipeManager.jsx`'s shape) and renders the combined list read-only, no form. Deliberately kept as local component state rather than lifted into `App` — correctly reasoned, unprompted, that only this component ever reads the grocery list, unlike `recipes` (Section 9's bug was specifically about *two* components sharing one copy of data). The `key` took three attempts: `item.name` alone (a real latent bug — two different units of a same-named ingredient would collide, since `GROUP BY` only guarantees `name`+`unit` unique together), then `item.name`+`item.quantity` (still wrong — `quantity` is the `SUM()` result, not a grouped column, so no uniqueness guarantee), landing correctly on `item.name`+`item.unit`, matching the actual `GROUP BY` clause. Confirmed live: 14 items rendered, no manual refresh needed. Section 12 (bug fix, found by the user 2026-08-28): the `useEffect([])` fetched only once on mount, so reassigning a day's recipe elsewhere never refreshed the grocery list — self-diagnosed unprompted, reasoning directly from the empty-array mechanic. Fixed by receiving `week` as a prop and swapping `[]` for `[week]`, so the fetch reruns whenever the week actually changes. Confirmed live: reassigning a day now updates the grocery list immediately. |
| `frontend/src/components/Login.jsx` | known | Moved into `components/` (Section 14, task 14.4), self-authored. Authored (task 6.6): a controlled password form; `handleSubmit` (self-authored) `POST`s to `/api/login` with `credentials: 'include'`, calling the `onLogin` callback prop on success or setting an error on failure. Task 8.1: self-authored swapping the hardcoded `http://localhost:3000` for `` `${import.meta.env.VITE_API_URL}/api/login` `` — first file converted, correct on the first try, became the template for the rest of the sweep. |
| `frontend/.env` | known | Authored (task 8.1): a single `VITE_API_URL=http://localhost:3000` key. The `VITE_` prefix is required for Vite to expose it to browser code at all (`import.meta.env.VITE_API_URL`) — anything without that prefix stays invisible to the frontend bundle. Git-ignored via the root `.gitignore`'s blanket `.env` rule (matches at any depth, not just `backend/`). |
| `frontend/src/index.css` | **deleted** | Was Vite's default global stylesheet (typography, dark/light theme CSS variables, `#root` width cap, `body` margin reset). Deleted (Section 18, task 18.2, 2026-08-31) at the student's explicit request, wanting a genuine blank slate to style the whole app personally rather than build on an unfamiliar scaffold theme. Its removal directly caused (and explained) two real, since-fixed bugs: the day row losing its `#root` width cap (desired), and an exposed default `<ul>` left-padding on `.week` that `body { margin: 0; }` used to silently absorb the effect of. |
| `frontend/src/assets/` | parked | Default scaffold image assets. |
| `frontend/README.md` | parked | Vite's boilerplate readme. |
| `frontend/dist/` | generated | Build output from `npm run build` (used when deploying/testing the production build, e.g. task 8.4). Ignored by git, regenerable. |

---

*Last updated: 2026-08-30 — Section 16 closed out (task 16.6): committed
and pushed as `87357aa`. `RecipeManager.jsx`'s instructions field is now
a real `<textarea>` instead of a single-line `<input>`, self-authored.
Confirmed live: Enter inserts a line break instead of submitting the
form (a real difference between the two elements' browser defaults), and
a real multi-line recipe's line breaks survive a full save → Edit-reload
round trip unchanged. `RecipeView.jsx`'s cookbook page now actually
renders those line breaks too — a `className` on the instructions `<p>`
plus a `white-space: pre-line` rule in `App.css`, both self-authored —
closing the gap between "the data is really multi-line" (16.2) and "it
visibly looks multi-line" (16.3). The textarea itself is styled too
(`.instructions-input { height: 4em; resize: vertical; }`), correctly
predicted to allow height-only resizing. Task 16.5's end-to-end pass
surfaced one real, quickly-corrected mix-up between the Edit form's
`<textarea>` and the separate cookbook-view component. No backend
changes anywhere in this section — `instructions` was already a plain
text column that round-trips newlines unchanged.

Section 15 closed out (task 15.7): committed
and pushed as `539f39c`. `backend/.gitignore` extended to exclude the 71
vendor AI-assistant reference doc files `prisma init` had scattered
around (task 15.1) — real files, but not code this project wrote or
maintains, so kept out of version control the same way `node_modules`
is.

Task 15.6 before it: the first
real Prisma Migrate workflow, closing out a future initiative flagged
back in task 10.3. New `backend/prisma/migrations/` holds two real
migrations — a manually-baselined `0_init` (adopting Migrate on a
database that already had real tables/data, via `migrate diff
--from-empty` + `migrate resolve --applied`, careful to never re-run
`CREATE TABLE` against existing tables) and a genuine
`add_recipe_created_at` migration adding `recipes.created_at`. Both
applied to `meal_planner` *and* `meal_planner_test` after a real 3-test
failure showed a migration only reaches whichever database
`DATABASE_URL` points at. Along the way: confirmed live that Postgres
backfills a new column's `DEFAULT` onto every existing row immediately
(not left blank), and that the generated Prisma Client is a frozen
snapshot needing an explicit `prisma generate` to see a schema change,
not something a server restart alone fixes.

Task 15.5 before it: the raw `pg`
`Pool` is fully retired. `backend/lib/db.js` deleted, `pg` uninstalled
from `package.json` (stays in `node_modules` only as `@prisma/adapter-pg`'s
own transitive dependency), and every remaining `pool` reference removed
from `app.js`/`app.test.js`/the three route files. Along the way, a real
bug was deliberately triggered and fixed to see it firsthand: with the
route files' `pool` parameter dropped before `app.js`'s calls were
updated to match, JS's positional (not by-name) argument binding meant
each file's `prisma` parameter actually caught the leftover `pool`
argument — reproduced live via a real `TypeError` and 3 failing tests,
fixed by updating `app.js`'s calls. That live click-through then
surfaced a second, unrelated real bug: `week.js`'s `PUT /api/week/:day`
was receiving `recipeId` as a string (every `<select>`'s value is text)
and Prisma's typed client rejected it, where the old raw SQL had
silently tolerated the same mismatch — fixed with `parseInt(recipeId)`.
Confirmed via all 6 tests and a real second click-through.

Task 15.4 before it: every write
route (`createRecipe`, `PUT /api/recipes/:id`, `PUT /api/week/:day`) is
now on Prisma — most of it found already converted and working at the
start of this task, undocumented until now; `DELETE /api/recipes/:id`
was the one real piece of new work, self-authored correct on the first
try past a correctly-predicted foreign-key check. Confirmed via all 6
tests plus a live create/update/delete `curl` cycle. `pool` is now
unused in both `recipes.js` and `week.js`, flagged for task 15.5.

Tasks 15.2-15.3 before it: a new
`backend/lib/prisma.js` provides one shared Prisma Client (driver-adapter
plumbing, guide-authored); all 3 read-only routes (`GET /api/week`, `GET
/api/grocery-list`, `GET /api/recipes`) now query through it instead of
raw SQL, each confirmed against real data. Along the way: a real
inner-JOIN-vs-`include` behavior gap self-diagnosed and fixed in
`week.js`, first uses of `.flatMap()`/`.reduce()`/`.sort()` in
`groceryList.js` to hand-roll the `GROUP BY`+`SUM()` Prisma's query
builder can't express across a join, a real Decimal-object addition
gotcha found and fixed with `parseFloat()`, and a second "Jest won't
exit" open-handle regression (the new Prisma connection) fixed the same
way task 7.3 fixed the first one. `requireAuth`'s local-dev bypass
(task 15.2) finished on the new `LOCAL_DEV_BYPASS_AUTH` flag.

Task 15.1 before it:
Prisma adopted — `backend/prisma/schema.prisma` introspected from the
real database (past a real dead-column fix), Prisma Client generated
and confirmed querying real data, past a pre-release-version mishap, an
accepted dev-only vulnerability, a TS-only generator swapped for the
legacy one, and a new mandatory driver-adapter requirement. `prisma
init` also auto-installed ~80 files of vendor AI-assistant reference
docs, parked in bulk. One real regression discovered right after, only
half-fixed: `middleware/requireAuth.js`'s local-dev bypass broke since
`DATABASE_URL` is no longer Render-only; a new `LOCAL_DEV_BYPASS_AUTH`
flag was added to `.env`, but the middleware itself doesn't check it
yet — local dev currently needs a real login again until that's
finished.

Section 14, task 14.6 before it: every
backend route (`routes/*.js` and `createRecipe()`) converted from
`.then()`/`.catch()` to `async`/`await` + `try`/`catch`, closing out a
future initiative flagged in task 10.3. Confirmed via all 6 tests, live
checks on every route, and a full create/update/delete cycle. The long
SQL queries then reformatted across multiple lines with backtick
template literals, on request.

Task 14.5 before it: full
confirmation pass — all 6 tests, plus every real user flow clicked
through in one session after the whole reorganization.

Task 14.4 before it: the frontend
now has its own `frontend/src/components/` folder too
(`RecipeManager.jsx`, `RecipeView.jsx`, `GroceryList.jsx`, `Login.jsx`),
entirely self-authored. `App.jsx`'s three imports fixed past two
self-caught bugs; the moved components' own import of each other needed
zero changes, correctly reasoned after one initially-wrong guess.
Confirmed live: the app renders correctly end to end.

Task 14.3 before it (plus a follow-up extension): `backend/app.js` shrank from ~200 to ~55 lines.
Four new `backend/routes/` files (`auth.js`, `week.js`,
`groceryList.js`, `recipes.js`), each an `express.Router()` built by a
factory function, mounted back into `app.js` — the one critical
ordering rule (login before `requireAuth`) preserved correctly.
`requireAuth` itself then moved to `backend/middleware/requireAuth.js`,
and the `Pool`/`poolConfig` setup to `backend/lib/db.js` — both entirely
self-authored, each past self-caught bugs correctly diagnosed once
asked to trace them. `app.js` then reorganized (requires grouped at
top, `app.use()` calls grouped together, relative order preserved).
Confirmed via tests and live checks throughout.

Task 14.2 before it (plus a follow-up move): the ingredient-parsing
logic now lives in `backend/lib/recipeParser.js` — the backend's first
real subfolder — wired back into `app.js` past a real require-path bug,
confirmed via tests.

Task 14.1 before it: the 3 remaining stale `TODO(you)` comments removed
from `backend/index.js` and `backend/app.js`, each verified correct
before removal; all 6 tests and a live `curl` confirmed nothing broke.

Section 13, task 13.6 before that: end-to-end confirmation — a fresh real
recipe imported through the UI, correctly appearing in the list and
rendering in `RecipeView`'s cookbook page.

Task 13.5 before it: real
"Import from URL" control in `frontend/src/RecipeManager.jsx`, a real
crash reproduced and fixed, further backend hardening in
`backend/app.js` (a fourth JSON-LD shape, plus a real nested-parens
regex bug found and fixed), and `SUPPORTED_SITES.md` corrected twice
more.

Task 13.4 before it: the import
route now actually saves real recipes to the database, sharing its
insert logic with `POST /api/recipes` via a new `createRecipe()`
function. Confirmed via `npm test` and a real live import.

Task 13.3 before it:
`backend/app.js` gained its first-ever regex and a real ingredient-line
parser (`{quantity, unit, name}`), confirmed against 10 real ingredient
lines including Unicode fraction quantities.

Task 13.2 before it:
`backend/app.js`'s `/api/recipes/import-from-url` route now finds the
real `Recipe` object in the JSON-LD and reduces it to
`{title, ingredientsRaw, instructions}`, confirmed via `curl`. Task 13.1
before it: the route itself, fetching an external recipe URL and
extracting its JSON-LD via `cheerio` (now a real
`backend/package.json` dependency).

Section 12 (bug fix, found by the user):
`GroceryList` now takes `week` as a prop and refetches whenever it
changes, instead of once on mount — fixes staleness after a day
reassignment. Section 11 before it: new
`frontend/src/RecipeView.jsx` (the app's first purely presentational,
hook-free component) renders a recipe as a real cookbook page, wired
into `frontend/src/RecipeManager.jsx` via a new `viewingRecipe` state
and styled in `frontend/src/App.css`. Section 10 closed out before it:
`frontend/src/GroceryList.jsx` fetches and displays the combined grocery
list; `backend/app.js` gained a new `GET /api/grocery-list` route and a
local-dev auth bypass; `backend/package.json` gained `nodemon` + a `dev`
script. `frontend/dist/` parked as generated build output.*
