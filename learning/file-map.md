# File map

Every file and folder in this repo, one line each on what it is and why it
exists. Nothing in here should stay a mystery box.

## Status definitions

- **known** — I've explained what it is and why it exists, in my own words.
- **parked** — honest one-liner for now; a real deep-dive is scheduled for
  later (usually the build-plan section that introduces it).
- **generated** — machine-made. Never hand-edit; regenerated by a tool.

---

## `learning/`

Not part of the app itself — this is the record of the learning process:
what the project is, how it'll be built, what's actually understood so far,
and what every file in the repo does.

| Path | Status | What it is / why it exists |
|---|---|---|
| `learning/project.md` | parked | Who I am, the project idea, and the In-MVP / Parking-lot scope split. Deep dive: whenever scope is revisited. |
| `learning/plan.md` | parked | The five locked stack decisions and the 8-section build plan, sections only (no tasks yet). Deep dive: start of each new section. |
| `learning/knowledge-graph.md` | parked | Living map of what's actually known vs. just seeded, per concept — drives what gets quizzed. Deep dive: after the first real quiz. |
| `learning/file-map.md` | parked | This file. Deep dive: whenever a new top-level file/folder is added and needs explaining. |

## `.claude/`

Claude Code's own project-level config — not app code, not part of
`learning/` either. Holds tooling for how I'm used on this project.

| Path | Status | What it is / why it exists |
|---|---|---|
| `.claude/commands/next-task.md` | parked | A saved slash command (`/next-task`) — the full "read the plan, execute one task, teach as you go" prompt, so it doesn't need retyping/pasting each time. I wrote it at your request; deep dive whenever you want to read or tweak the wording yourself. |

## `backend/`

The Express server (Section 3), started from scratch with `npm init` (task 3.1) — no scaffolding tool, unlike `frontend/`. Sits alongside `frontend/` as its own separate project.

| Path | Status | What it is / why it exists |
|---|---|---|
| `backend/package.json` | known | Authored via `npm init` (task 3.1); now lists Express as a dependency (task 3.2). Task 7.2: gained a `devDependencies` block (Jest, Supertest, installed via `--save-dev`) separate from `dependencies`, and `scripts.test` now runs `jest` instead of the `npm init` placeholder. |
| `backend/index.js` | known | Authored solo (tasks 3.3–6.2): Express server with full recipe CRUD (`GET`/`POST`/`PUT`/`DELETE /api/recipes`) and `PUT /api/week/:day` for day assignment; `/api/week` uses a `JOIN` across the split `recipes`/`week_meal` schema. All routes tested for real via `curl`. Task 6.5: added `POST /api/login` (`bcrypt.compare` against the stored hash, self-authored) and a `requireAuth` middleware (guide-authored) gating every route registered after it — reuses the same middleware-ordering trick as `cors`. Task 6.6: `cors()` narrowed from wildcard to `{ origin: 'http://localhost:5173', credentials: true }` (guide-authored) so the frontend's session cookie is actually accepted cross-origin. Task 7.1: shrunk to just `const app = require('./app'); app.listen(...)` — self-authored, including catching (after a prompt) a first attempt that called `require('./app')` without assigning it to anything. |
| `backend/app.js` | known | Authored (task 7.1): all the Express app/route setup that used to live in `index.js`, minus `app.listen` — ends with `module.exports = app` instead, so Supertest can hand requests straight to the app object in tests without a real server needing to be running. Content is the guide's mechanical relocation of code the student already wrote; the split itself (and the resulting `index.js`) is what's new. Task 7.3: now also exposes `app.pool = pool` (guide-authored) so tests can close the DB connection pool when they're done. Task 7.5 aside: self-spotted that `POST /api/recipes` returned `200` instead of the more correct `201 Created` for a route that makes a new resource; self-authored the one-line fix (`res.status(201).json(...)`) and updated the matching test expectation. Task 7.7: self-authored an `if (process.env.NODE_ENV === 'test')` conditional around the `dotenv.config()` call — loads `.env.test` under Jest, `.env` otherwise — correct on the first try, no fixes needed. Task 8.1: self-authored swapping the hardcoded CORS origin for `process.env.CORS_ORIGIN`, correct on the first try. |
| `backend/.env.test` | known | Authored (task 7.7): same 7 keys as `.env`, `DB_NAME` changed to `meal_planner_test`. Loaded instead of `.env` whenever `NODE_ENV=test` (which Jest sets automatically). Git-ignored via the root `.gitignore`'s `.env` rule alongside the real one. Task 8.1: gained `CORS_ORIGIN`, kept in sync with `.env` even though tests don't exercise CORS. |
| `backend/.env.render` | known | Authored (task 8.2): one key, `RENDER_DATABASE_URL` — the External Database URL for the new `meal-planner-db` Render Postgres instance, used to migrate schema/seed data and confirm connectivity from the local machine via `psql`. Automatically caught by the `.env.*` `.gitignore` rule added in task 7.8, with zero extra edits needed — that fix already paying off. Not meant to be read by the deployed app itself (which will use Render's *Internal* URL instead, wired in task 8.3); this file is a local-only admin/migration credential. |
| `backend/app.test.js` | known | Authored (task 7.2): first real test — `request(app).get('/api/week')` with no session, asserting a `401`. Self-authored and self-debugged (a missing space turned `await request` into one undeclared identifier, `awaitrequest`). Task 7.3: added two more self-authored tests (wrong password rejected; correct password + `request.agent(app)` unlocks a protected route), a test-only password hash (`process.env.ADMIN_PASSWORD_HASH` overwritten with a throwaway test password — the real password never enters this file), and `afterAll(() => app.pool.end())` to close the DB pool after tests (fixed a real "Jest did not exit" warning; correctly diagnosed the open `pg` connections as the cause before being told). Task 7.4: added a fourth test asserting `GET /api/week`'s response *shape* (array of `{day, meal}` objects) rather than exact values, self-authored past one real bug — first attempt called `.toHaveProperty('day')` on the whole array instead of `response.body[0]`, correctly predicted the failure and self-fixed after seeing the real Jest diff. Task 7.5: added a fifth test — the first *mutation* test (inserts a real DB row via `POST /api/recipes`), self-authored the `toBe`/`toHaveProperty` assertions on the create response; guide wrote the follow-up `GET /api/recipes` + `.find()` persistence check and the `DELETE` cleanup at the end (new syntax: `.find()`, template-literal URL) so the test doesn't leave junk data behind — confirmed via a real `psql` query after the run that the row was really gone. Task 7.6: added a sixth test — `PUT /api/week/:day` actually reassigns a day. First test needing "restore" cleanup instead of "delete" cleanup (a day's row always exists; only its recipe changes), guide-authored; self-authored the core assertion (`expect(mondayAfter.meal).toBe(newRecipe.title)`), correct on the first try. Confirmed via a real `psql` query after the run that Monday was back to "Beef Tacos" and the temp recipe was gone. Aside (8/16): self-spotted the repeated `agent = request.agent(app); await agent.post('/api/login')...` boilerplate across 4 tests as a DRY violation; guide added a module-scoped `beforeEach` to log in fresh before every test, self-removed the 3 now-redundant duplicate blocks (tests 4–6) in the editor, correctly leaving tests 1–3 untouched since they specifically test unauthenticated/login behavior itself. Mispredicted the resulting pass count (3/6, forgetting tests 1–2 don't use `agent` and test 3 declares its own local one) but correctly diagnosed the exact gap once asked. |
| `backend/.env` | known | Authored (task 5.3): DB connection credentials (user/password/host/port/database name). Task 6.5: added `ADMIN_PASSWORD_HASH` (self-generated via `bcrypt.hashSync`, real password never shared with the guide) and `SESSION_SECRET` (guide-generated random hex, signs the session cookie). Task 8.1: added `CORS_ORIGIN` (currently `http://localhost:5173`, will become the deployed frontend's URL later). Git-ignored via the root `.gitignore`'s `.env` rule — never committed. |
| `backend/package-lock.json` | generated | Created by `npm install express` (task 3.2), updated by later installs (`pg`, `bcryptjs`, `express-session`). Exact locked dependency versions. Never hand-edit. |
| `backend/node_modules/` | generated | Installed dependencies (Express, pg, bcryptjs, express-session, and their dependency trees). Ignored by git, regenerable. |

## Project root

| Path | Status | What it is / why it exists |
|---|---|---|
| `.git/` | generated | Created by `git init` (task 1.1). Git's own storage for the entire commit history of this project. Hidden (dot-prefixed), never hand-edit — only Git itself writes here. |
| `.gitignore` | known | Authored (task 1.2): tells Git to never track `node_modules/` (regenerable via npm) or `.env` (secrets). Set up before either file exists. Task 6.6: added `cookies.txt`, after a stray `curl -c cookies.txt` cookie-jar file from testing the login route turned up untracked at the repo root; the file itself was deleted (disposable test output, not app code). Task 7.8: added `.env.*`, after a near-miss where the new `backend/.env.test` (real secrets, same shape as `.env`) turned up untracked and would have been committed — the plain `.env` line only ever matched that exact filename. Self-authored past one gap (first draft, `.env.*` alone, doesn't also cover the bare `.env` file) and independently rejected a broader one-line `.env*` fix as over-matching unrelated dotfiles. |
| `index.html` | known | Authored solo (task 1.5): the first real app page — a hardcoded fake week of recipes, plain HTML, no CSS/JS yet. Section 2 rebuilds this in React. |
| `/` (repo root) | parked | Holds `learning/`, `.claude/`, `.git/`, `index.html`, and `frontend/`. Section 1's static page lives at root; the React rebuild (Section 2) lives in `frontend/`. |

## `frontend/`

The React app (task 2.1), scaffolded with Vite. Separate folder from the repo root on purpose — `backend/` will sit alongside it in Section 3, keeping frontend/backend as distinct concerns.

| Path | Status | What it is / why it exists |
|---|---|---|
| `frontend/package.json` | parked | Project manifest — lists dependencies (React, Vite) and npm scripts (`npm run dev`, etc.). Deep dive: whenever we add a new dependency. |
| `frontend/index.html` | parked | Stripped-down HTML shell with an empty `<div id="root">` and a script tag loading `src/main.jsx`. React fills that div at runtime. |
| `frontend/src/main.jsx` | parked | Entry point — finds `#root` and tells React to render the app into it. |
| `frontend/src/App.jsx` | known | Authored (tasks 2.2–4.4): week-of-recipes JSX; `week` is real backend-fetched state (`useEffect` + `fetch`), CORS unblocked — genuinely live end-to-end. Task 6.3: now also renders `<RecipeManager />` below the week list (first multi-component composition; the import/render wiring was written by the guide, not authored solo). Task 6.4: each day now has a `<select>` dropdown (built from fetched `recipes` state) that `PUT`s an assignment via `handleAssign` and re-`fetchWeek()`s to refresh — authored solo, correct on the first try, no bugs. Also self-extracted `fetchRecipes` as a named function mirroring `fetchWeek`, unprompted. Task 6.6: gated behind `loggedIn` state — an early-return `if (!loggedIn) return <Login .../>` and a `useEffect` dependency array changed from `[]` to `[loggedIn]` (both guide-authored); all three of its own `fetch` calls gained `credentials: 'include'` (`fetchWeek` self-fixed, `fetchRecipes`/`handleAssign` guide-swept). Task 8.1: all three `fetch` calls' hardcoded `http://localhost:3000` swapped for `` `${import.meta.env.VITE_API_URL}` `` (guide-swept, same pattern the student proved solo in `Login.jsx`). |
| `frontend/vite.config.js` | parked | Tells Vite this is a React project so it knows how to handle JSX. |
| `frontend/.gitignore` | parked | Vite-generated, scoped to this subfolder — redundant with the root `.gitignore`'s `node_modules/` rule but harmless. |
| `frontend/package-lock.json` | generated | Exact installed dependency versions. Never hand-edit. |
| `frontend/node_modules/` | generated | Installed dependencies (via `npm install`). Ignored by git, regenerable. |
| `frontend/.oxlintrc.json` | parked | Config for Oxlint, the linter chosen during scaffolding. |
| `frontend/public/` | parked | Static assets served as-is (favicon, icons). |
| `frontend/src/App.css` | known | Authored (tasks 2.3–2.4): `.week`/`.day` flexbox card layout, plus `.day.selected` for the click-to-highlight state (yellow border). |
| `frontend/src/RecipeManager.jsx` | known | Authored (task 6.3): full CRUD UI for recipes — fetches and lists `/api/recipes`, a controlled form (create + edit, same form/handler reused via `editingId` state) doing real `POST`/`PUT`/`DELETE` calls. Debugged two real bugs solo: a `setRecipe`/`setRecipes` typo caught via a browser-console `ReferenceError`, and a missing `else` that was silently double-submitting on edit (POST *and* PUT both firing). Task 6.6: all four `fetch` calls gained `credentials: 'include'` (guide-authored sweep) so they keep working once the app is behind the login gate. Task 8.1: all four `fetch` calls' hardcoded `http://localhost:3000` swapped for `` `${import.meta.env.VITE_API_URL}` `` (guide-swept, same pattern the student proved solo in `Login.jsx`). |
| `frontend/src/Login.jsx` | known | Authored (task 6.6): a controlled password form; `handleSubmit` (self-authored) `POST`s to `/api/login` with `credentials: 'include'`, calling the `onLogin` callback prop on success or setting an error on failure. Task 8.1: self-authored swapping the hardcoded `http://localhost:3000` for `` `${import.meta.env.VITE_API_URL}/api/login` `` — first file converted, correct on the first try, became the template for the rest of the sweep. |
| `frontend/.env` | known | Authored (task 8.1): a single `VITE_API_URL=http://localhost:3000` key. The `VITE_` prefix is required for Vite to expose it to browser code at all (`import.meta.env.VITE_API_URL`) — anything without that prefix stays invisible to the frontend bundle. Git-ignored via the root `.gitignore`'s blanket `.env` rule (matches at any depth, not just `backend/`). |
| `frontend/src/index.css` | parked | Vite's default global stylesheet (typography, dark mode, `#root` width) — untouched so far, still just explained, not authored. |
| `frontend/src/assets/` | parked | Default scaffold image assets. |
| `frontend/README.md` | parked | Vite's boilerplate readme. |

---

*Last updated: 2026-08-17 — task 8.2 (`backend/.env.render` added; a real
Render Postgres database now exists, schema + seed migrated).*
