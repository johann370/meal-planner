# Knowledge graph

The map of what I actually know, not what the project needs. This is what
decides what I get quizzed on — I should never be re-quizzed on something
`understood` and still fresh, and nothing upgrades status without real
evidence of something I said or did.

## Status definitions

- **seed** — not yet taught.
- **introduced** — explained to me once. No check yet.
- **practicing** — I've used it myself, with help/guidance.
- **understood** — I explained it in my own words *and* passed a quiz on it.

Statuses only move forward on evidence — a specific thing I said or did,
recorded in the Evidence column. Downgrades are fine too, if a later quiz
shows the understanding didn't stick.

Everything walked through and checked during planning (`project.md`,
`plan.md`) starts at **introduced**, not higher — a good verbal explanation
in a planning chat isn't the same as using the thing or passing a real quiz.

---

## Low-level (syntax, code mechanics)

| Concept | Status | Introduced | Last reviewed | Evidence |
|---|---|---|---|---|
| Variables | seed | — | — | — |
| Data types (string/number/boolean) | seed | — | — | — |
| Functions | seed | — | — | — |
| Conditionals (if/else) | seed | — | — | — |
| Loops (for/while) | seed | — | — | — |
| Arrays | understood | 2026-08-14 | 2026-08-14 | Built `[monday, tuesday, ...]` in `backend/index.js`; predicted `.map()` would work on a plain object, confirmed the real `TypeError` themselves in the console, then restructured to a proper array and correctly described the result as "items with indices" |
| Objects | understood | 2026-08-14 | 2026-08-14 | Same exercise — correctly distinguished object *keys* (the original `{monday: {...}}` shape) from array *items containing* `day`/`meal` properties, in their own words, after debugging the `.map()` gap |
| DOM manipulation (select/update elements) | seed | — | — | — |
| Event listeners (clicks, etc.) | practicing | 2026-08-14 | 2026-08-14 | Wired working `onClick` handlers on all 7 day cards, each correctly calling the `useState` setter with its own day name |
| React components, JSX, props, state | understood | 2026-08-14 | 2026-08-14 | Authored `useState`, an `onClick` setter, and a conditional-`className` ternary across all 7 days. Predicted (wrongly) that a hardcoded-vs-dynamic `className` bug on 6/7 days would still work; found via DOM inspection that state updating doesn't automatically propagate; fixed all 6 correctly, unprompted. Review quiz 8/14: unprompted, correctly explained *why* a plain variable can't hold state ("resets back to the original value after each re-render") |
| `useEffect` (side effects, dependency array) | understood | 2026-08-14 | 2026-08-14 | Authored `useEffect(() => {...}, [])` correctly on first attempt to fetch data once on mount. Review 8/14: unprompted, correctly predicted a browser refresh would be required for new backend data to appear, reasoning precisely from the empty dependency array meaning "fetch runs once on mount, nothing re-triggers it" |
| CORS (cross-origin requests, why browsers block them; cookies need explicit `credentials` opt-in) | understood | 2026-08-14 | 2026-08-18 | Hit a real "Access-Control-Allow-Origin missing" error firsthand fetching `localhost:3000` from `localhost:5173`. Fixed via `cors` middleware; correctly predicted (with reasoning) that `app.use(cors())` must be registered *before* the route, since Express runs middleware top-to-bottom in registration order. Reinforced 8/16: correctly predicted, unprompted, that cross-origin cookies need `credentials: 'include'` on *every* request, not just login, before being told — confirmed via a real 401 in the console. Reinforced 8/18 (task 8.4): correctly predicted, unprompted, that the deployed frontend's new `onrender.com` origin wouldn't match the backend's still-`localhost`-configured `CORS_ORIGIN`, and that requests would get blocked — extending the concept from a local dev mismatch to a real production one. Task 8.5: fixed the predicted mismatch by updating `CORS_ORIGIN` on Render, hit a second real CORS failure from a trailing `/` on the value (an origin is `scheme://host:port` only — never a path — so it must match *exactly*, character for character), correctly located the extra slash once pointed at it, fixed it, and confirmed live login + a DB-backed fetch both working end-to-end across two different deployed HTTPS origins |
| CORS vs. `SameSite` cookies as two separate browser rules (CORS: is JS allowed to *read* a cross-origin response; `SameSite`: does the browser *attach* a cookie to the request at all) | introduced | 2026-08-18 | 2026-08-18 | Task 8.5's plan anticipated needing explicit cookie `SameSite`/`Secure` settings for two different `onrender.com` subdomains, on top of the `CORS_ORIGIN` fix — turned out unnecessary, verified live rather than assumed (`express-session`'s config never changed, and the session cookie still worked across origins). Explained why: different subdomains are different *origins* (CORS applies) but still the same *site* for cookie purposes, so the default cookie behavior was already permissive enough — not yet self-explained |
| OS-level installs (`apt`) vs. project-scoped `npm` packages | understood | 2026-08-14 | 2026-08-14 | Correctly predicted PostgreSQL would install system-wide, not into the project folder like `node_modules` — unprompted, before running anything |
| Postgres authentication (peer vs. password/TCP auth) | practicing | 2026-08-14 | 2026-08-14 | Set a real password via `ALTER USER postgres PASSWORD ...` (learned it reports back as `ALTER ROLE`, `USER` being a legacy alias) specifically because peer auth (used by `psql` via `sudo -u postgres`) doesn't apply to Node's TCP connection |
| Node `pg` client (`Pool`, connection config, query results) | understood | 2026-08-14 | 2026-08-14 | Authored a working `Pool` config and a real test query; added `err` handling unprompted. Reinforced 8/14: wired `pool.query(...).then(result => res.json(result.rows))` into the real `/api/week` route, unprompted removal of all now-dead fake-data code, correctly reasoned that an extra `id` field wouldn't break the frontend |
| systemd services (`active (exited)`, `pg_isready` as ground truth) | introduced | 2026-08-14 | 2026-08-14 | Hit the confusing "active (exited)" status line; predicted it was fine (right instinct, imprecise reasoning); verified with `pg_isready` and had the umbrella-unit mechanism explained |
| Route parameters (`req.params`) vs. request body (`req.body`) | understood | 2026-08-14 | 2026-08-14 | Authored `:id`/`:day` route params and destructured `req.body` correctly across 3 different routes without confusing the two |
| Parameterized SQL queries (`$1`/`$2`, injection prevention) | understood | 2026-08-14 | 2026-08-14 | First attempt used named placeholders (`$title`) instead of positional (`$1`); predicted it'd work the same, hit a real Postgres syntax error, fixed it, then correctly used positional params unprompted in every route afterward |
| `curl` for testing non-GET routes | understood | 2026-08-14 | 2026-08-14 | Used `curl -X POST/PUT/DELETE` with JSON bodies to test every CRUD route for real before any frontend existed. Review quiz 8/14: unprompted, correctly explained a plain browser URL visit only ever sends `GET`, so it couldn't have tested the `POST` route |
| Express middleware (`app.use`, pipeline ordering) | understood | 2026-08-14 | 2026-08-14 | Authored `app.use(cors())` correctly ordered before the route it needed to affect. Review quiz 8/14: unprompted, correctly explained why `express.json()` must precede routes that read `req.body`; separately gave a complete, accurate trace of a full PUT request's lifecycle (route match → handler → DB update → response) |
| Rendering lists in React (`.map()` + `key` prop in JSX) | understood | 2026-08-14 | 2026-08-28 | Refactored 7 hardcoded `<li>` blocks into one `week.map(...)`, also generalizing the `className` ternary and `onClick` to use dynamic `day.day` instead of hardcoded strings — unprompted. Explained `key` unprompted, in own words: "gives an identifier for react for each object, without it react would have to reconstruct the list every time". Reinforced 8/28 (task 10.5): self-authored the first *nested* list in the app — a `.map()` over `recipe.ingredients` inside the existing `.map()` over `recipes` — using `ingredient.id` (a real DB id) as the key instead of an array index for the first time. Correctly predicted, unprompted, that the key choice has no visual effect on what renders, only on React's internal bookkeeping — confirmed live. Reinforced again same day (task 10.7): this time the data (`/api/grocery-list`'s `GROUP BY` result) had no `id` column at all. First attempt used `item.name` alone; correctly predicted, when asked, that a duplicate key would make React "silently misbehave, not crash" — not yet triggered by today's data, but a real latent bug (two different units of the same-named ingredient would collide). Took two more tries to land the actual fix: `name`+`quantity` was still wrong (`quantity` is the `SUM()` *result*, not a grouped column, so it carries no uniqueness guarantee), landing correctly on `name`+`unit` once pointed back at the query's own `GROUP BY` clause — the real takeaway being that a key's uniqueness has to be tied to what the data actually guarantees, not just any-two-fields-that-look-distinctive |
| CSS selectors: class vs. element/tag (styling scope) | understood | 2026-08-14 | 2026-08-14 | Review quiz 8/14: asked why a plain `li` selector would be risky; reasoned to the correct answer themselves — "if we add any li in the future they would get the same styling, which we might not necessarily want" |
| async/await and `fetch` | understood | 2026-08-14 | 2026-08-16 | Authored a correct `fetch(...).then(res => res.json()).then(data => setWeek(data))` chain inside `useEffect`, unprompted, no syntax errors. Reinforced 8/16 with real `async`/`await` syntax (previously only `.then()` chains): authored `const response = await request(app).get(...)` in a Jest test, debugged a real `awaitrequest` typo via a precise `ReferenceError`, understood exactly why |
| HTTP verbs (GET/POST/PUT/DELETE) | understood | 2026-08-14 | 2026-08-16 | Authored working `GET`/`POST`/`PUT`/`DELETE` routes for `/api/recipes` plus `PUT /api/week/:day`, all tested for real via `curl`. Correctly predicted (before testing) that deleting an FK-referenced recipe would be blocked, and it was. Reinforced 8/16: authored real `fetch` calls for all four verbs from the frontend (not just `curl`), including building the `POST`/`PUT` options object (`method`/`headers`/`body`) from scratch |
| SQL basics (SELECT/INSERT/UPDATE/DELETE) | understood | 2026-08-14 | 2026-08-25 | Authored `CREATE DATABASE`, `CREATE TABLE`, `DROP TABLE`, `INSERT`, `UPDATE`, `DELETE`, and `SELECT`, both via `psql` and through real Express routes. Review quiz 8/14: correctly reasoned that `RETURNING *` on a `DELETE` for a nonexistent `id` would come back as an empty `rows` array, and that the current route doesn't actually check for that case. Reinforced 8/25 (task 10.1): self-authored `CREATE TABLE ingredients (...)`, correct on the first try, unprompted mirroring of `week_meal`'s existing pattern. Self-authored the follow-up `INSERT`, past two real bugs in one attempt (double-quoted string literals, an extra closing paren) — see the two rows below for how each was caught |
| SQL aggregation (`GROUP BY` collapses every row sharing the same value(s) in the grouped column(s) into one bucket; an aggregate function like `SUM()` combines a numeric column *within* each bucket) | practicing | 2026-08-28 | 2026-08-28 | Task 10.6: shown live in `psql` — an ungrouped 3-way `JOIN` returned 4 separate "chicken breast, 1 pound" rows (one per assigned day using a chicken recipe); adding `GROUP BY ingredients.name, ingredients.unit` + `SUM(ingredients.quantity)` collapsed them to one "4 pounds" row. Correctly predicted, twice, before running: the grouped row count would drop (21→14) and the chicken breast total specifically (4), both confirmed live. Self-transcribed the same query into the real `GET /api/grocery-list` route's `TODO(you)` blank, correct on the first try. The exact-text-match gotcha ("Pound" vs "pound" not combining) was guide-explained, not yet self-noticed |
| SQL relationships (foreign keys, joins) | understood | 2026-08-14 | 2026-08-27 | Split one conflated table into `recipes` + `week_meal` (`recipe_id INTEGER REFERENCES recipes(id)`). Correctly predicted, unprompted, that Postgres blocks dropping a referenced table. Authored a `JOIN` query and correctly predicted its exact 7-row output (day + title pairs) before running it. Reinforced 8/25 (task 10.1): before writing any code, correctly reasoned unprompted that the same ingredient name used in two different recipes means *two separate rows*, not one shared row — distinguishing "what this recipe needs" from a master ingredient catalog. Self-authored a new `ingredients` table with its own `recipe_id` foreign key, then a confirming `JOIN` query; first attempt had a stray comma between `FROM ingredients` and `JOIN recipes`, correctly predicted (once pointed at it) that it would error before running; fixed it, then correctly predicted the exact single-row output (`Spaghetti \| 1 \| Pound \| Spaghetti Bolognese`) before running — confirmed exactly right. Reinforced 8/26 (task 10.3): correctly predicted, unprompted, that `DELETE /api/recipes/:id` would now fail with a real FK violation once recipes had actual `ingredients` rows — generalizing the same blocked-delete concept from `week_meal` (Section 6) to a second, brand-new foreign key relationship — confirmed live via a real 500 response, then fixed by deleting the child `ingredients` rows first. Reinforced 8/27 (task 10.3 test-suite debugging): given a raw `week_meal`/`recipes` dump showing Monday's `recipe_id` as NULL, correctly reasoned that an **inner** `JOIN` drops any row whose foreign key doesn't match — so Monday's row vanishes from `/api/week`'s result entirely, rather than appearing with a blank `meal` field — explaining a real `undefined` bug in the test suite from first principles |
| SQL string literals vs. identifiers (single quotes `'...'` for text values, double quotes `"..."` for column/table names) | understood | 2026-08-25 | 2026-08-26 | Task 10.1: first `INSERT` attempt wrote string values in double quotes (`"Spaguetti"`, `"Pound"`), which Postgres reads as a reference to a column/table name, not text. Didn't self-spot the distinction — asked directly ("do i need single quotes?") once pointed at the two differing quote characters — but correctly applied the fix afterward, rewriting the full statement with single quotes on the next attempt, no further errors. Task 10.2: self-authored a fresh multi-row `INSERT` (2 more ingredient rows for `recipe_id=2`, mixing quoted text values and a bare numeric `quantity`) solo, correct on the first try — no quoting mistake this time, and correctly predicted the follow-up `SELECT`'s exact row count (3) before running it |
| Password hashing/comparison (bcrypt, salting) | understood | 2026-08-16 | 2026-08-16 | Correctly predicted a *plain* hash of the same input would repeat, then correctly revised the prediction after being taught about salting ("they will be different"), confirmed live via a real two-hash bcrypt demo. Generated the real `ADMIN_PASSWORD_HASH` themselves via `bcrypt.hashSync`. Authored the `/api/login` route's `bcrypt.compare(...).then(isMatch => ...)` check solo, correct on the first try, tested successfully end-to-end via `curl` |
| Sessions & cookies (stateful auth: session ID cookie, server-side "logged in" memory) | practicing | 2026-08-16 | 2026-08-27 | Concept explained (HTTP is stateless; a signed cookie plus server-side memory is how "already logged in" persists across requests). The `express-session` middleware/`requireAuth` wiring was guide-authored, not yet self-written — but correctly predicted, and then confirmed via real `curl -c`/`-b` cookie-jar calls, that a session cookie from `/api/login` would unlock a previously-401'd route. Reinforced same day in a new context: authored a Supertest test using `request.agent(app)` (the automatic equivalent of `curl -c`/`-b`) to log in and then hit a protected route with the same session, correct on the first try. Reinforced 8/27 (task 10.3): hit the real consequence live, twice — restarting the local server (to pick up code edits) wiped its in-memory session store, so the existing `cookies.txt` session immediately started getting real `401`s until logging in again each time |
| Test assertions (Jest's `test()` + `expect().toBe()`, `.not.toBe()`, `.toHaveProperty()`) | understood | 2026-08-16 | 2026-08-16 | Authored a real Supertest+Jest test solo: `const response = await request(app).get('/api/week'); expect(response.status).toBe(401);`. Debugged a real `ReferenceError: awaitrequest is not defined` (missing space between `await` and `request`) via precise console output, reasoning correctly that a keyword glued to an identifier just becomes one undeclared identifier. Reinforced same day: authored two more tests solo (wrong-password rejection, correct-password-then-protected-route via `request.agent(app)`), including `.not.toBe(401)`, all correct on the first try. Reinforced 8/16 again: authored `Array.isArray(response.body)` correctly unprompted; first `toHaveProperty('day')` attempt checked the whole array instead of `response.body[0]`, correctly predicted it would fail, self-fixed by indexing after reading the real Jest diff (`Received path: []`). Reinforced once more 8/16: self-authored `expect(createResponse.body.title).toBe(newRecipe.title)` and `toHaveProperty('id')` on a POST response, correct on the first try |
| Testing structural "shape" vs. exact values (why a route backed by live/changeable data gets a `toHaveProperty`/`Array.isArray` check instead of an exact-value assertion) | practicing | 2026-08-16 | 2026-08-16 | Explained the distinction before writing the test (values from the DB can change; the *contract* — array of `{day, meal}` objects — doesn't). Self-authored the array-shape assertions past one real bug (see Test assertions row) |
| JS arrays are objects (numeric-index/`length` properties, not arbitrary named ones) | introduced | 2026-08-16 | 2026-08-16 | Predicted `toHaveProperty('day')` on an array would fail, reasoning "it's an array not an object" — corrected: arrays *are* objects in JS (that's why `.length` works), the real gap is that this array's keys are `0`..`6`/`length`, not `day`. Correctly restated afterward as "it does not have a property day" |
| Jest lifecycle hooks (`afterAll`, `beforeEach`, setup/teardown around tests) | practicing | 2026-08-16 | 2026-08-16 | Authored `afterAll(async () => { await app.pool.end(); })` solo, correctly, after being guided to it as the fix for a real "Jest did not exit" warning. Reinforced 8/16: unprompted, correctly derived `beforeEach` (mirroring `afterAll`'s "runs once, after everything" as "runs fresh, before each individual test") when asked to reason from the name. Guide authored the `beforeEach` block itself; self-removed the 3 redundant duplicate login blocks it replaced, correctly leaving the 3 tests that test auth/login behavior itself untouched |
| Variable scope & shadowing (a `let` declared outside a function vs. a `const` of the same name declared inside one); block scoping (`const`/`let` declared inside `{ }` is invisible outside it) | understood | 2026-08-16 | 2026-08-27 | Mispredicted the pass count after the `beforeEach` refactor (guessed 3/6, forgetting tests 1–2 never reference `agent` at all and test 3 declares its own local `const agent` that shadows the outer one). Correctly self-diagnosed the exact gap once asked to walk through the reasoning. Task 8.3: wrote `poolConfig` as a separate `const` inside each branch of an `if/else`; correctly predicted, unprompted and before running, that line 26's `new Pool(poolConfig)` (outside both blocks) would fail because "it's defined inside the if statement, it can't be read outside of it" — then self-fixed it (hoisted a single `let poolConfig;` above the `if/else`, assigned rather than redeclared inside each branch), correct on the first try. Task 10.3 (`PUT`): generalized the exact same fix to a brand-new case unprompted — `const updatedRecipe` declared inside one `.then()` callback, referenced in a later *sibling* `.then()` callback (not nested, chained); correctly predicted the resulting `ReferenceError` before running, confirmed live via a real `curl` 500 response, then self-fixed by naming "hoist a `let updatedRecipe;` above the chain" without being shown the fix, only reminded of the parallel to `poolConfig` |
| Testing mutations + cleanup (a test that writes real data must undo it, so runs stay repeatable and the real table doesn't fill with junk) | practicing | 2026-08-16 | 2026-08-16 | Explained before writing task 7.5's test: `POST /api/recipes` inserts a real DB row, unlike prior read-only tests. Guide wrote the cleanup `DELETE` call and the follow-up `GET` + `.find()` persistence check; correctly predicted, before running, that the cleanup delete would succeed — confirmed via a real `psql` query afterward showing 0 matching rows. Generalized 8/16 to a second cleanup shape for task 7.6: correctly predicted, unprompted, that `PUT /api/week/:day` needs "restore the original value" cleanup rather than "delete what you made" — reasoning precisely that a day's row always exists, only its assigned recipe changes — confirmed via `psql` afterward |
| Array `.find()` (locate one item in an array matching a condition) | introduced | 2026-08-16 | 2026-08-16 | Seen in guide-authored code (`listResponse.body.find(recipe => recipe.id === createResponse.body.id)`) as part of task 7.5's persistence check — not yet self-written |
| HTTP status code semantics (`200 OK` vs. `201 Created` vs. others as *meaning*, not just "success/fail") | understood | 2026-08-16 | 2026-08-16 | Unprompted, spotted that `POST /api/recipes` returned `200` and asked whether `201 Created` would be more correct for a route that makes a new resource — the right convention, reasoned to independently, not something the plan asked for. Self-authored the fix (`res.status(201).json(...)` in `app.js`) and updated the test's expectation to match |
| Resource cleanup / open handles (why a process won't exit until things like DB connections are closed) | understood | 2026-08-16 | 2026-08-16 | Correctly diagnosed, unprompted, that the `pg` `Pool`'s open database connections were what kept Jest's process alive after tests finished — reasoned to this before being told. Fix (`app.pool.end()` in `afterAll`) confirmed working: the warning was gone on rerun |
| CSS layout: Flexbox (`display:flex`, `flex-wrap`, `justify-content`, `flex: 1`) | practicing | 2026-08-14 | 2026-08-14 | Authored `.week`/`.day` flex layout independently; predicted resize behavior, observed real shrink-before-wrap mechanic (flex-basis:0 + min-width:auto floor), gap explained |
| CSS block-level centering (`margin: 0 auto` — a block element's `margin` defaults to `0`, which is *why* it hugs the left edge on its own; setting left/right margins to `auto` instead splits the container's leftover horizontal space evenly between both sides) | practicing | 2026-08-28 | 2026-08-28 | Section 11: self-authored `.recipe-view`'s `border`/`max-width`/`padding` (mirroring `.day`) correctly, but `max-width: 80%` alone left it stuck to the left. Correctly reasoned, when asked, that a block element defaults left; the fix itself (`margin` as the relevant property, `auto` as the centering value) was guide-explained, not self-derived — but then self-refined from the shorthand `margin: auto` to the more explicit `margin: 0 auto` unprompted, confirmed live |
| HTML document structure (doctype, head/body, tags, valid nesting) | practicing | 2026-08-14 | 2026-08-14 | Authored `index.html` independently (no scaffold used) — valid doctype/head/body, 7 correctly-nested `<li><h2><p>` day entries, all real content |
| Block-level default rendering (headings/paragraphs stack; one bullet per `<li>`) | introduced | 2026-08-14 | 2026-08-14 | Predicted 2 lines per day (correct) and bullet placement (guessed "spans both lines," actual is "anchors to first line only") — gap explained |
| Controlled form inputs / elements (`value`/`onChange`, `<select>`+`<option>`) | practicing | 2026-08-16 | 2026-08-16 | Introduced via `<input>` (guide-authored, not yet self-written). Reinforced same day: authored a `<select onChange=...>` with `recipes.map()` into `<option>` elements solo, correct on the first try — generalized the pattern to a new element type without being shown it first |
| Immutable array updates (spread to append, `.filter()` to remove, `.map()` to replace-in-place) | practicing | 2026-08-16 | 2026-08-16 | Authored `setRecipes([...recipes, newRecipe])` on create and `setRecipes(recipes.filter(recipe => recipe.id !== id))` on delete (the delete one self-written past a hint, correctly reasoning to check `response.status === 204` first); the replace-in-place `.map()` on edit was copied from a hint with a leftover `: r` typo, self-diagnosed and fixed after a console error |
| Debugging via browser DevTools console errors | practicing | 2026-08-16 | 2026-08-16 | Guided to check the console for the first bug (`setRecipe is not defined`), read the exact error, and correctly reasoned that the POST had already reached the server before the broken `.then()` ran — the response handling failed, not the request. Found and fixed a second, similar typo (`: r` → `: recipe`) independently |
| Multi-component composition (splitting UI into separate components, importing/rendering one inside another) | practicing | 2026-08-16 | 2026-08-28 | `RecipeManager` split out of `App` as a second component; the guide wrote the import + `<RecipeManager />` wiring in `App.jsx` and explained why (separate concerns, `App` doesn't need to know how recipe management works internally) — not yet self-authored. Reinforced 8/18: diagnosing a real bug (stale recipe list in the day-assignment dropdown) required reasoning about *why* two sibling components — `App` and `RecipeManager` — each having their own separate `recipes` state causes them to drift out of sync. Reinforced 8/28 (task 10.7): a third component, `GroceryList`, self-authored solo (own `useState`/`useEffect`/`fetch`, mirroring `RecipeManager`'s shape) — import + render wiring in `App.jsx` still guide-authored, matching the established mechanical-step split. Reinforced again same day (Section 11): a fourth component, `RecipeView`, introduced a new shape — a purely *presentational* component with no hooks at all, all data arriving via props (`recipe`, `onClose`) from `RecipeManager`. Wired in via the same `if (condition) return <Component .../>` early-return pattern already seen in `App.jsx`'s login gate. One real bug caught before ever running: the title's `onClick` initially called `setViewingRecipe(true)` instead of `setViewingRecipe(recipe)` — self-spotted and fixed once asked to trace what `RecipeView` would do with a boolean instead of the real object |
| Lifting state up (two components needing the same data share *one* copy of it, owned by their common parent, passed down as props — including the setter function itself, not just the value) | understood | 2026-08-18 | 2026-08-19 | Diagnosing the stale-dropdown bug, correctly reasoned unprompted that `RecipeManager`'s existing `setRecipes(...)` calls would stop working if `recipes` became a read-only prop — then, from one guiding question, correctly concluded "you would have to pass both recipes and setRecipes" down from `App`. Correctly recognized `RecipeManager`'s existing immutable-update logic (`[...recipes, newRecipe]`, `.filter()`, `.map()`) wouldn't need to change at all — only where the state itself lives. Task 9.1: self-authored `<RecipeManager recipes={recipes} setRecipes={setRecipes} />` in `App.jsx`, correct on the first try; correctly predicted (once redirected from a premature guess about a missing-`setRecipes` crash) that passing the props then would have *no visible effect yet*, since `RecipeManager`'s function signature didn't take a `props` parameter at all — reasoning precisely that unread props just arrive and sit unused. Task 9.2: self-authored the other half solo, ahead of guidance — changed `RecipeManager`'s signature to `({recipes, setRecipes})`, deleted its own now-redundant `useState`, and (once prompted to remove the matching fetch) deleted the whole `useEffect` plus its now-unused `useEffect` import, unprompted. Correctly predicted, before running, that the visible recipe list would look identical afterward "since its the same data, just being retrieved in a different spot" — correctly separating *where data lives* from *what renders*. Task 9.3: correctly predicted, unprompted, that the day-assignment dropdown would now update live with no refresh, reasoning "since the recipes are being passed down as a prop" — confirmed live locally (backend + frontend both run for the first time this session), closing the loop on the original 8/18 stale-dropdown bug end to end. Reinforced 8/28 (task 10.7): applied the concept in reverse, correctly and unprompted — reasoned that the new `GroceryList` component *doesn't* need its state lifted into `App`, since it's the only component that will ever read it, correctly identifying that the original bug was specifically about *two* components sharing one copy of data, not a general rule that all state must live in `App` |
| Extracting reusable functions (naming shared logic instead of duplicating it) | practicing | 2026-08-16 | 2026-08-16 | Guide pulled the week fetch out into a named `fetchWeek()` (used by both the initial `useEffect` and, later, `handleAssign`) and explained why. Self-generalized the same move unprompted: wrote a matching `fetchRecipes()` function rather than an inline fetch, mirroring the pattern without being told to |
| Environment-based config switching (reading `NODE_ENV` to pick which `.env` file `dotenv` loads, via its `path` option) | understood | 2026-08-17 | 2026-08-17 | Reasoned through the chicken-and-egg problem unprompted (can't read `NODE_ENV` *from* `.env` to decide which file to load, since deciding requires reading it first) once asked to trace the order of operations — correctly concluded `NODE_ENV` must come from *outside* the file (Jest sets it directly). Self-authored the full `if (process.env.NODE_ENV === 'test') {...} else {...}` conditional around `dotenv.config()` in `app.js`, correct on the first try |
| Vite client-side env vars (`import.meta.env.VITE_*`, the `VITE_` allowlist prefix, values baked in at dev-server-start/build time not read live) | understood | 2026-08-17 | 2026-08-18 | Quizzed on *why* Vite restricts client env vars to a `VITE_` prefix instead of exposing the whole `.env` like `process.env` does; correctly reasoned unprompted ("only exposes the variables that it needs so it doesn't accidentally leak something else") — sharpened with the browser-bundle angle (frontend env vars ship to every visitor, unlike the backend's `.env` which never leaves the server). Self-authored the first conversion (`Login.jsx`'s fetch call, hardcoded string → `` `${import.meta.env.VITE_API_URL}/api/login` ``), correct on the first try; the same proven pattern was then sub-swept across `RecipeManager.jsx` (4 calls) and `App.jsx` (3 calls). Correctly predicted, unprompted, that a newly-created `.env` requires a dev-server restart rather than picking up via HMR — reasoning precisely that env values are injected once at server start, not watched like source files. Reinforced 8/18 (task 8.4): correctly reasoned, unprompted, that a Render Static Site's build without `VITE_API_URL` set would need something in place *before the build ran* — confirmed live via a real local build with the var unset, revealing the deployed bundle would call `fetch(\`undefined/api/login\`)` (not "localhost", as first guessed) |
| `dotenv`'s missing-file behavior (a missing `path` file loads silently with 0 vars injected, no thrown error) | introduced | 2026-08-17 | 2026-08-17 | Predicted a thrown "file not found" error when `.env.test` didn't exist yet; actual behavior was silent (`injected env (0) from .env.test`, logged but not fatal) — the real crash surfaced one layer downstream, in `express-session` refusing an undefined secret. Explained after the gap was found via a live repro; not yet self-explained back |
| Test database isolation (why tests need their own database, not just their own cleanup code) | understood | 2026-08-17 | 2026-08-27 | Correctly predicted, unprompted reasoning, the exact real-data consequence of a test crashing before its cleanup line runs (Monday stays pointed at the temp recipe, the temp recipe itself survives) — before being told this was the whole point of the task. Live-discovered a real stray "Test Recipe" row already sitting in the production `recipes` table (leftover from a prior, pre-isolation test run) as concrete proof of the exact risk just reasoned through, and directed its deletion. Reinforced 8/27 (task 10.3): the *test* database itself (not prod) turned up 5 orphaned junk recipes plus Monday's real NULL `recipe_id` — leftovers from some earlier crashed run. Correctly reasoned, unprompted, that "some error in the tests probably caused the leftover rows and they were never cleaned up as a result" — recognizing that isolation protects production from this, but doesn't stop the test database from accumulating the exact same kind of cruft when a test itself crashes before its own cleanup |
| Dynamic port binding for cloud hosts (`process.env.PORT` vs. a hardcoded port) | understood | 2026-08-18 | 2026-08-18 | Correctly predicted, unprompted and before running anything, that a hardcoded `app.listen(3000, ...)` would break on Render because "it wont be routed to the correct port, so it wont reach the api" — reasoning precisely that Render assigns its own port via `PORT` and routes public traffic there. Self-authored the fix (`if (process.env.PORT) {...} else {...}` in `index.js`) from a plain-language spec, correct on the first try — confirmed live by starting the server with `PORT=5050` and getting a real `401` back from that exact port |
| JS type coercion into strings (a non-string value used somewhere a string is expected gets silently `.toString()`'d rather than erroring — `undefined` in a template literal becomes the literal word `"undefined"`; a plain object becomes `"[object Object]"`) | introduced | 2026-08-18 | 2026-08-27 | Predicted a missing `VITE_API_URL` at build time would make the deployed frontend call out to "the localhost url" — actual behavior demonstrated live (a real local build with the var unset) was `fetch(\`undefined/api/login\`)`. Gap explained: `.env` is git-ignored so it was never actually in play, and `undefined` in a template literal stringifies rather than vanishing. Reinforced 8/27 (task 10.4): predicted a JSX `<input value={anArray}>` would error when `ingredients` became an array; actual result, self-observed live in the browser, was React rendering the literal text `[object Object]` instead — the connection back to this same coercion concept was explained, not yet self-drawn |
| Object spread (`{...obj, key: newVal}` — copies every key from `obj` into a new object; a key repeated afterward overwrites the spread-in one) | introduced | 2026-08-26 | 2026-08-26 | Task 10.3 (`GET /api/recipes`): shown a real key collision — `recipe` already had a stale `ingredients` string key, then `{...recipe, ingredients: [...]}` set it again. Correctly predicted, unprompted, that "the one that comes after" (the array) wins — confirmed live via the actual `curl` response. Syntax itself was guide-authored, not yet self-written |
| Implicit globals (assigning to an undeclared identifier — no `const`/`let`/`var` — doesn't throw in non-strict/CommonJS code; it silently creates a variable on the global object instead) | introduced | 2026-08-26 | 2026-08-26 | Task 10.3: wrote `insertPromises = ingredients.map(...)` with no declaration; predicted (wrongly) it would be `undefined`/throw an error. Real behavior demonstrated live via a minimal repro (`foo() { mysteryVar = 42 } foo(); console.log(mysteryVar)` → prints `42` outside the function) — gap explained: it "worked" by accident, leaking into global scope instead of failing loudly |
| Arrow function implicit vs. explicit return (`x => expr` returns `expr` automatically; `x => { expr }` with braces does *not* return anything unless you write `return` yourself) | practicing | 2026-08-26 | 2026-08-26 | Task 10.3: wrote `.map(ingredient => { pool.query(...) })` with braces and no `return`; guessed the result would be "an object," corrected via a live repro (`[1,2,3].map(n => { n*10 })` → `[undefined, undefined, undefined]`). Self-corrected immediately afterward to a working return-free expression body (`ingredientsResult.map(ingredient => ingredient.rows[0])`), correct on the first try, no further errors |
| `Promise.all()` (waiting for a whole array of promises — often built via `.map()` over a variable-length list — to all finish before continuing, instead of waiting on just one) | practicing | 2026-08-26 | 2026-08-26 | Task 10.3 (`POST /api/recipes`): needed to insert an unknown-length list of ingredient rows and wait for all of them before responding. Self-authored `Promise.all(insertPromises)` from a plain-language comment naming the tool (not shown the syntax); reused it correctly, unprompted, in a second spot after the concept was explained once |
| Nested promise chains must `return` to propagate errors (a `.then()` inside another `.then()` that isn't `return`ed becomes a separate, disconnected chain — its rejections never reach the outer `.catch()`) | practicing | 2026-08-26 | 2026-08-26 | Task 10.3: a real `TypeError` inside an un-returned inner `Promise.all(...).then(...)` became an unhandled rejection and **crashed the whole Node process** (confirmed via `ps aux` showing it gone, and the stack trace in the server log) instead of hitting the outer `.catch()`'s 500 response. Explained via the real crash; self-fixed by adding `return` in `POST`, then correctly generalized the same fix unprompted, first try, to the analogous spot in `DELETE /api/recipes/:id` from just a hint comment ("return that chain") |
| Array `.map()` fundamentals (always returns exactly one output per input item; each callback call is independent — no memory of previous iterations, so it can't be used to *accumulate* a shrinking/growing list the way spreading into a setter can) | introduced | 2026-08-26 | 2026-08-26 | Task 10.3: first attempt at unwrapping `Promise.all`'s results tried `ingredientsResult.map(ingredient => [...newRecipe.ingredients, ingredient.rows[0]])` — misapplying the "spread to append" pattern from Section 6 inside a `.map()`, which would have produced nested arrays. Explained the distinction (transform vs. accumulate); self-corrected next attempt to a plain one-to-one `.map()`, correct and clean |
| `pg`'s parameter serialization for mismatched column types (a JS array/object sent as a query param for a `text` column doesn't error — `pg` converts it into Postgres's own array-literal syntax, `JSON.stringify`-ing any object elements along the way, producing a garbled-but-"successful" write) | introduced | 2026-08-27 | 2026-08-27 | Task 10.3 (`PUT`): predicted sending an `ingredients` array against the still-unfixed `UPDATE ... ingredients = $2` (a text column) would throw an error. Real behavior, seen live via a real `curl` response, was a silent garbled write (`"{\"{\\\"name\\\":...\"}"}`) instead — gap explained: the mismatch is real, but `pg` papers over it by serializing rather than rejecting |
| Arrow function implicit return, applied unprompted a second time (switching *back* to explicit `{ return ... }` the moment a second statement — a variable declaration — was needed, after having correctly used the bare `() => expr` form for a single-statement case moments earlier) | practicing | 2026-08-26 | 2026-08-27 | Task 10.3 (`PUT`): self-authored `.then(() => pool.query(...))` (implicit return, correct, unprompted) for the ingredients-delete step; when the next revision needed to also declare `updatedRecipe`, self-switched to `.then((result) => { updatedRecipe = result.rows[0]; return pool.query(...) })` — braces plus explicit `return` — without being told the syntax needed to change, correctly reasoning the implicit form no longer applied once there were two statements |

## Structural (how files and pieces connect)

| Concept | Status | Introduced | Last reviewed | Evidence |
|---|---|---|---|---|
| `package.json` and npm dependencies | understood | 2026-08-14 | 2026-08-14 | Ran the Vite scaffold, saw `frontend/package.json` land with dependencies pre-filled; then ran `npm init` from scratch for `backend/`, correctly predicting most fields but expecting a dependency-install question that doesn't exist — corrected: dependencies are always a separate `npm install <pkg>` step. Review quiz 8/14: unprompted, correctly explained *why* 67 packages installed from one `npm install express` (recursive dependency trees) |
| `node_modules` | practicing | 2026-08-14 | 2026-08-14 | Watched `npm install` run during scaffolding, then confirmed via `git check-ignore` that `frontend/node_modules` is correctly ignored. Reinforced 8/14: ran `npm install express` directly, predicted only `package.json` would change, discovered the gap — 67 packages installed (dependency trees), `package-lock.json` created fresh |
| Modules (import/export) | understood | 2026-08-14 | 2026-08-27 | Authored correct CommonJS `require()` in `backend/index.js` (vs. ESM `import` in `frontend/`); explained in own words that syntax differs, then correctly identified `package.json`'s `"type"` field as what determines which one applies to a given project. Reinforced 8/16: extended to local relative requires (`require('./app')` vs. requiring a package by name) and a custom `module.exports` value (the `app` object) — first attempt called `require('./app')` without assigning the result, caught after being asked to predict the outcome. Reinforced 8/27 (task 10.4): wrote `import Fragment from 'react'` (default-import syntax) for `Fragment`, which is actually a *named* export — hit a real "element type is invalid" error. Once pointed at line 1's `import { useState } from 'react'` for comparison, correctly identified the fix ("import it next to useState") |
| Project folder structure (frontend vs. backend as separate folders) | practicing | 2026-08-14 | 2026-08-14 | The anticipated `backend/` folder is now real — created via `npm init`, sitting alongside `frontend/` as its own independent project with its own `package.json` |
| Vite dev server & HMR (hot module reload) | understood | 2026-08-14 | 2026-08-14 | Observed `hmr update /src/App.jsx` in the terminal on save; browser updated without a manual refresh, mechanism explained. Review quiz 8/14: unprompted, correctly explained HMR only reloads what changed rather than the whole page |
| Client-server communication (request/response) | practicing | 2026-08-13 | 2026-08-14 | API explained as the fixed contract of requests the frontend can send and responses it gets back. Reinforced 8/14: ran `backend/index.js`, correctly predicted the process would stay alive (`app.listen`), then made a real browser request to `localhost:3000` and got the actual response back |
| Frontend/backend as separate concerns | understood | 2026-08-13 | 2026-08-14 | Own words: "it also clearly separates the frontend from the backend, so i can learn each one independently". Review quiz 8/14: initially framed the frontend/backend port split as a security boundary; redirected to the tooling angle, then correctly reasoned that Vite and Express are different tools for different jobs and unprompted connected it back to the Express-over-Next.js decision |
| Environment variables (`.env` files) | understood | 2026-08-13 | 2026-08-14 | Authored the `.gitignore` entry for `.env`; first draft explained it as "varies per environment," revised after a prompt to the sharper reason (secrets exposure). Reinforced 8/14: authored a real `backend/.env` with live DB credentials, correctly called `dotenv.config()` before constructing the `Pool` that reads from `process.env` |
| Hidden files / dotfiles (e.g. `.git`) | practicing | 2026-08-13 | 2026-08-13 | Predicted `git init` would show "no changes"; corrected the gap by reasoning through where history must be stored, then ran `ls -a` themselves and found `.git` |
| `.gitignore` pattern syntax (trailing slash = folder; `*` wildcard matching) | understood | 2026-08-13 | 2026-08-17 | Wrote `.env/` (folder syntax) for what's actually a file; caught and fixed it themselves after being asked to distinguish file vs. folder. Reinforced 8/17: real near-miss — `backend/.env.test` wasn't covered by the existing `.env` line and would have been committed with live secrets. First proposed `.env.*`; correctly reasoned, when asked, that the required literal second dot means `.env.*` does *not* also cover the bare `.env` file. Then independently rejected the simpler one-line `.env*` fix on its own merits — unprompted, correctly identified it would also swallow unrelated dotfiles like a future `.envrc` — and chose the more precise two-line fix (`.env` + `.env.*`) |
| Git branches as movable pointers (vs. commit hashes as content fingerprints) | introduced | 2026-08-14 | 2026-08-14 | Predicted `git branch -M main` would change the commit hash; ran it, saw the hash was unchanged, and the pointer-vs-fingerprint distinction was explained to close the gap |
| Git remotes (`origin`, fetch/push, `-u`/upstream tracking) | understood | 2026-08-14 | 2026-08-14 | Ran `git remote add origin ...`, correctly predicted `git remote -v` would show the GitHub URL, then ran `git push -u origin main` themselves and got a real push through. Review quiz 8/14: unprompted, correctly explained `-u` links local `main` to `origin/main` so future plain `git push`/`pull` know where to go |
| SSH host verification (known_hosts, fingerprint check) | introduced | 2026-08-14 | 2026-08-14 | Hit the first-time host-authenticity prompt on push; had it explained as a one-time trust check, accepted it correctly |

## Engineering practice

| Concept | Status | Introduced | Last reviewed | Evidence |
|---|---|---|---|---|
| Git & version control (init/commit/push) | understood | 2026-08-13 | 2026-08-14 | Quizzed "what does Git protect": correctly distinguished explicit/deliberate commits from autosave, and tied that to reliable reverting. Reinforced 8/14: correctly predicted `git status` mid-stage, post-commit clean state, and `git log --oneline` output before running each; correctly predicted `git add .` stages both untracked and modified files together; committed/pushed the full Section 2 React app solo, unprompted, no mechanics questions needed |
| Databases as persistent storage | understood | 2026-08-13 | 2026-08-18 | Own words, re: relational shape of the data: "since there a lot of links/relationships in the data for my app, a relational database...would be better". Reinforced 8/14: correctly predicted, unprompted, that a direct `psql INSERT` would show up on plain browser refresh with **no** backend restart — precisely because Express re-queries live instead of holding data in memory (the exact opposite of task 4.5's in-memory case) — then confirmed it live. Reinforced 8/18 (task 8.6): full MVP loop confirmed on the live public site — created a real recipe, assigned it to a day, refreshed twice, both the new recipe and the day assignment survived — proof the deployed Render Postgres database, not browser/server memory, is what's actually holding the data |
| Node script execution vs. Vite's dev tooling (no built-in file-watching) | understood | 2026-08-14 | 2026-08-28 | Correctly predicted `node index.js` would need a manual restart to pick up file changes, unlike Vite's HMR; confirmed by restarting themselves. Reinforced 8/14 in the same breath as the `useEffect` prediction, correctly separating "backend needs restart" from "frontend needs refresh" as two distinct reasons. Reinforced 8/28 (task 10.6): hit it live and unprompted this time — a real `curl` against the new `/api/grocery-list` route came back `Cannot GET /api/grocery-list` because the running server was still serving pre-edit code; restarted it correctly once reminded of the mechanism. Reinforced again same day: when asked directly whether switching to `npm run dev` (nodemon) would still wipe the session cookie on save, correctly predicted yes — the deeper reasoning (nodemon automates the restart, doesn't eliminate it) was guide-explained, but the outcome prediction itself was correct before being told |
| Choosing PostgreSQL specifically | understood | 2026-08-13 | 2026-08-13 | Quizzed "why Postgres": correctly named both the relational shape of the data (recipes↔days) and the industry/docs reason for Postgres specifically, unprompted |
| The backend/server's role | practicing | 2026-08-13 | 2026-08-14 | Explained as core component #3; discussed again during Decision 3 (Express). Reinforced 8/14: ran a real Express server locally and confirmed it alongside the React frontend, both live at once for the first time |
| The frontend/UI's role | introduced | 2026-08-13 | 2026-08-13 | Explained as core component #4 |
| Authentication / login gate | understood | 2026-08-13 | 2026-08-16 | Explained as core component #6; scoped explicitly into the MVP (single login, not full accounts). Backend built and tested 8/16 (see below). Frontend gate completed same day: correctly predicted, *unprompted and before testing*, that login would succeed but the week/recipe fetches would still 401 — reasoning precisely from cross-origin cookies needing explicit opt-in on every request, not just the login one. Confirmed live, then fixed one call site (`fetchWeek`) solo before the guide swept the remaining five |
| Callback props (passing a function down as a prop so a child can signal its parent) | practicing | 2026-08-16 | 2026-08-16 | `App` passing `onLogin={() => setLoggedIn(true)}` into `<Login>` was guide-authored and explained (state flows one direction, so a child triggers a parent update by calling a function it was handed). Used correctly in their own `handleSubmit` — called `onLogin()` only inside the `response.ok` branch, not unconditionally |
| `useEffect` dependency arrays beyond `[]` (re-running an effect when a specific value changes) | understood | 2026-08-16 | 2026-08-28 | `App.jsx`'s week/recipe fetch changed from `[]` (run once on mount) to `[loggedIn]` (run once, then again whenever `loggedIn` changes) — guide-authored and explained; not yet self-written, but correctly reasoned about its effect when predicting the login flow. Reinforced 2026-08-28 (Section 12, real bug the student found themselves): correctly self-diagnosed, unprompted, *why* `GroceryList` was going stale after a day reassignment — "an empty dependency array means it only runs once" — precise, own-words root cause, no hints given. Self-authored the fix (`GroceryList` receiving `week` as a prop, `[week]` replacing `[]`), then correctly predicted, unprompted and before testing, exactly why it would now work ("any time it is updated, the useEffect will run again") — confirmed live |
| Hosting & deployment | understood | 2026-08-13 | 2026-08-18 | Explained as core component #7; extensively discussed via Render-vs-AWS tradeoff in Decision 5. Task 8.3: correctly predicted, unprompted, that a code fix needed to be committed and pushed before a Render deploy would include it, reasoning "Render builds and runs your backend by pulling from GitHub, not... the files on your laptop." One correction needed: predicted a new env var would trigger a "rebuild," corrected to "restart" (no code changed, so no build step needed — same restart-to-pick-up-change idea already known from local Node dev, just applied to a cloud host). Task 8.4: three correct unprompted predictions in a row — env var timing relative to a build, the resulting CORS mismatch, and (once shown the real `undefined` bug) correctly tying it back to "it needs to be set before it runs". Task 9.4: correctly predicted, unprompted, that pushing the bug-fix commit would trigger a real *rebuild* (not just a restart) — reasoning "theres changes to the files in the github repo" — correctly distinguishing this from task 8.3's env-var-only case, which needed no build. Confirmed live: the fix (task 9.1-9.3) verified working on the deployed site after the rebuild completed |
| Render hosting types: Web Service (a long-running process, needed when something must keep running — e.g. Express) vs. Static Site (built once, then just served as files, no process to keep alive) | practicing | 2026-08-18 | 2026-08-18 | Explained the distinction before creating either: the backend became a Web Service (task 8.3) because Express is a long-running process; the frontend became a Static Site (task 8.4) because `npm run build` just produces plain HTML/CSS/JS files with nothing that needs to keep running. Executed the Static Site creation (Root Directory/Build Command/Publish Directory) with guidance; not yet asked to explain the distinction unprompted |
| Configuration / secrets management | understood | 2026-08-13 | 2026-08-18 | Quizzed via the `.gitignore` `.env` reasoning: initial draft gave a weaker reason, revised to correctly tie it to preventing secrets exposure when prompted to reconcile with earlier own-words explanation. Reinforced 8/18: correctly identified that Render's deployed secrets live as dashboard-configured environment variables (the cloud equivalent of a local `.env` file) rather than anything committed to the repo |
| Debugging a deployed service (server-side errors surface in the host's logs, not your local terminal, since the process isn't running on your machine) | understood | 2026-08-18 | 2026-08-18 | Correctly reasoned, unprompted, that the same `console.error(err)` that prints to a local terminal would instead land in "render console" — reasoning precisely that the server isn't running locally anymore. Used the real Render Logs tab to find the actual error (`getaddrinfo ENOTFOUND <full-connection-string>`) behind a generic `500 Internal server error`, rather than guessing at the cause |
| Cloud-hosted database (Render Postgres): Internal vs. External connection URLs as a real network/security boundary, not just convenience | practicing | 2026-08-17 | 2026-08-18 | Correctly chose External URL for a laptop-to-cloud connection, reasoning "so you can test stuff on your own machine as well" — the *why two URLs exist at all* (Internal never reaches the public internet; External is secured only by its embedded credentials) was explained, not yet self-derived. Correctly predicted, unprompted, that connecting would succeed but `SELECT * FROM recipes` would fail on a brand-new empty database — confirmed live. Task 8.3: correctly picked Internal URL for the deployed web service talking to the deployed database (both live inside Render's network) when asked directly, though the question carried most of the reasoning — not yet a full unprompted explanation |
| Database connection strings (`postgres://user:pass@host:port/db` as one URI) vs. discrete env-var fields (`DB_USER`/`DB_HOST`/etc.) | understood | 2026-08-17 | 2026-08-18 | Asked directly whether a separate username/password were still needed once handed the Render connection URL — correctly prompted the explanation that the URI already bundles everything `psql` needs in one string, unlike the discrete-flags approach used locally. Task 8.3: self-authored the real `if (process.env.DATABASE_URL) { poolConfig = {connectionString: ...} } else { poolConfig = {discrete fields} }` branch in `app.js` from a plain-language spec, no code shown — correct shape on the first attempt (only the block-scoping wrapper needed a fix, see Variable scope row) |
| Automated testing (why/what) | practicing | 2026-08-16 | 2026-08-16 | Installed Jest + Supertest, wrote and ran a real test against the exported Express `app` (no live server needed), got it to a passing green result — first real "safety net" test, verifying the login gate actually blocks an unauthenticated request |
| `devDependencies` vs. `dependencies` (npm) | introduced | 2026-08-16 | 2026-08-16 | Explained before installing: `dependencies` (Express, pg, ...) are needed to run the app; `devDependencies` (Jest, Supertest) only matter while developing/testing. Observed the split land correctly in `package.json` after `npm install --save-dev` |
| Scoping an MVP / deferring scope | introduced | 2026-08-13 | 2026-08-13 | Co-authored the In-MVP / Parking-lot split and working agreement in `project.md` |
| Linting as opt-in vs. bundled into dev server (CRA vs. Vite) | introduced | 2026-08-14 | 2026-08-14 | Correctly recalled CRA's auto-warnings from past experience; reconciled the gap by reading `package.json`'s scripts and seeing `lint` is separate from `dev` |
| Reading JS error messages (e.g. `ReferenceError`) | practicing | 2026-08-14 | 2026-08-28 | Predicted a vague error for `useState is not defined`; actual message named the exact identifier — precision of reference errors as a pattern was pointed out. Reinforced 2026-08-28 (dev tooling, ad hoc): asked to compare a bare `NODE_ENV !== 'test'` reference against `process.env.NODE_ENV === 'test'` a few lines above in the same file, correctly predicted a `ReferenceError` unprompted, reasoning "that variable doesn't exist" — correct diagnosis of *why* the pattern breaks, applied to a real (if fleeting — see `backend/app.js` file-map entry) in-between state of the file |
| Database migrations (versioned schema-change scripts, run by a tool against every environment and tracked so they're never double-applied — as opposed to hand-running `CREATE TABLE`/`ALTER TABLE` against whichever database happens to be open) | introduced | 2026-08-27 | 2026-08-27 | Task 10.3: after discovering `meal_planner_test` was missing the `ingredients` table (task 10.1's `CREATE TABLE` only ever ran against dev), asked unprompted "would there be a better way to do this to make the test database automatically update" — the exact problem migrations solve. Concept explained in response; flagged as a real future section in `plan.md`, not yet built |
| React Fragments (`<>...</>` shorthand groups sibling elements without an extra DOM wrapper; the shorthand accepts *no* props at all — a `key` requires the explicit `<Fragment key={...}>` form, imported by name from `react`) | practicing | 2026-08-27 | 2026-08-27 | Task 10.4: self-authored `<>...</>` unprompted to return 3 sibling inputs from one `.map()` iteration — correct, not previously shown. Hit a real "each child should have a unique key" warning because the key was misplaced on the 3 inner inputs instead of the fragment itself; once told the shorthand can't take a `key` at all, correctly moved it to an explicit `<Fragment key={index}>`, removing the now-redundant inner keys unprompted |
| HTML forms: a `<button>` inside a `<form>` defaults to `type="submit"` unless told otherwise — so any button without an explicit `type` fires the form's `onSubmit`, not just its own `onClick` | introduced | 2026-08-27 | 2026-08-27 | Task 10.4: clicking a hand-authored "Add Ingredient" button (no `type` set) unexpectedly fired a real `POST /api/recipes` request — self-noticed via the Network tab once asked to check it. Root cause explained; self-applied the `type="button"` fix to both the add and remove buttons afterward, unprompted for the second one |
| Immutable array updates, generalized to an array of *objects* with multiple fields (spread the target object, overwrite just one field, keep every other array item unchanged via `.map()`/`.filter()` by index rather than by `id`) | practicing | 2026-08-16 | 2026-08-28 | (See original entry above for Section 6 evidence — `id`-based `.map()`/`.filter()`.) Reinforced 8/27 (task 10.4): first attempt at an ingredient-name `onChange` returned the raw new string in place of the matched array item (`i === index ? e.target.value : ingredient`), silently replacing a whole object with a string; correctly predicted the resulting corrupted shape (`ingredients[0]` becomes a plain string) once asked to trace it, then self-fixed to `{...ingredient, name: e.target.value}` before ever running it. Independently generalized the same pattern to `quantity`, `unit`, a `.filter()`-by-index row-removal, and simplified an `indexOf`-based lookup to the `.map()` callback's own index parameter once it was pointed out |
| Producer/consumer shape matching (a value read from one place — e.g. an API response — flows correctly into another place — e.g. a form's `.map()` — with zero translation code, *only because* both sides already agree on the same field names/shape; not automatic, and not guaranteed) | practicing | 2026-08-28 | 2026-08-28 | Task 10.4's end-to-end check: predicted "both ingredients show up," then observed `handleEditClick`'s `setIngredients(recipe.ingredients)` correctly populate both ingredient rows on Edit — no code written for that in this task, contradicting `plan.md`'s note that it was known-broken. Asked *why* it worked unprompted; first answer described the render mechanism (`.map()` lists every item) but missed the actual cause. Second hint (compare the two shapes directly) got the right answer: `GET /api/recipes` (task 10.3) already returns `ingredients` as an array of `{name, quantity, unit}` objects, the exact shape the form expects — needed the hint, not fully unprompted |
| Debugging via the browser Network tab (inspecting which real HTTP requests actually fired, not just reading console errors) | introduced | 2026-08-27 | 2026-08-27 | Task 10.4: asked to check the Network tab after an unexplained crash; found and reported a real unexpected `POST /api/recipes` request, which was the key clue diagnosing the "button defaults to type=submit" bug |

## AI-era practice

| Concept | Status | Introduced | Last reviewed | Evidence |
|---|---|---|---|---|
| Context management | practicing | 2026-08-13 | 2026-08-13 | Ran `/context` themselves and discussed the token breakdown |
| Agent memory files | introduced | 2026-08-13 | 2026-08-13 | Watched a memory file get created live (`prefers-working-out-problems-first.md`) and had the mechanic explained |
| Model selection (Haiku/Sonnet/Opus tradeoffs) | practicing | 2026-08-13 | 2026-08-13 | Ran `/model` themselves and confirmed current model |
| Plan mode (review before execution) | practicing | 2026-08-13 | 2026-08-13 | Toggled plan mode themselves and observed the real enter/exit system events |
| Skills (packaged workflows) | practicing | 2026-08-13 | 2026-08-13 | Ran `/skills` and `/reload-skills` themselves; asked what `code-review` does and got an explanation |
| Writing a good plan | introduced | 2026-08-13 | 2026-08-13 | Co-built the MVP scope and the locked-decisions build plan this session, decision by decision |
| Reviewing a diff | introduced | 2026-08-13 | 2026-08-13 | Asked what `/code-review` does; learned it checks correctness plus simplification/efficiency, at selectable effort levels |

---

*Last updated: 2026-08-28 — Section 12 (bug fix, found by the user):
`GroceryList` was fetching once on mount and never refreshing, so
reassigning a day's recipe elsewhere left it stale. Root cause
self-diagnosed unprompted from the already-understood `useEffect([])`
mechanic; fix (passing `week` down as a prop, `[week]` replacing `[]`)
self-authored, with the end result correctly predicted before testing —
this concept moves from `introduced` to `understood`.

Section 11 (cookbook-style recipe view)
complete, tasks 11.1-11.4: a new `RecipeView.jsx`, the app's first
purely presentational component (no hooks, all data via props), wired
into `RecipeManager.jsx` via a new `viewingRecipe` state and an
early-return pattern echoing `App.jsx`'s own login gate. One real bug
(passing `true` instead of the actual `recipe` object to the state
setter) caught by reasoning before ever running the code. Styled with a
centered card layout, hitting a genuine CSS gap along the way — a block
element's default-left positioning, fixed via `margin: 0 auto` — and
self-refining from the shorthand to the more explicit form unprompted.
A scope question (multi-line instructions needing a `<textarea>`) was
raised and deliberately deferred, keeping the task tight. Confirmed live
end-to-end.

Section 10 (structured ingredients + grocery list) closed out the
session before it: `GroceryList.jsx` fetches and displays the combined
list; the backend's new `GET /api/grocery-list` route (`GROUP BY` +
`SUM()`, first SQL aggregation in the project) was demonstrated live in
`psql` before being wired in. Ad hoc dev-tooling work also landed:
`nodemon` auto-restarts the backend on save, and `requireAuth` bypasses
login in local dev only (gated on the same "not on Render" signal
`poolConfig` already uses, excluded from test mode) — full test suite
still passing. Four future initiatives now flagged, not yet built:
database migrations, refactoring the backend's `.then()` chains to
`async`/`await`, case-insensitive unit matching in the grocery list, and
a `<textarea>` upgrade for multi-line recipe instructions. Extending the
dev-login bypass to the frontend's own `loggedIn` state was raised and
explicitly declined — not a gap, a deliberate choice.*
